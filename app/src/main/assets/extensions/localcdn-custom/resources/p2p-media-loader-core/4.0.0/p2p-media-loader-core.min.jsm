//#region \0rolldown/runtime.js
var e = Object.create, t = Object.defineProperty, n = Object.getOwnPropertyDescriptor, r = Object.getOwnPropertyNames, i = Object.getPrototypeOf, a = Object.prototype.hasOwnProperty, o = (e, t) => () => (t || (e((t = { exports: {} }).exports, t), e = null), t.exports), s = (e, n) => {
	let r = {};
	for (var i in e) t(r, i, {
		get: e[i],
		enumerable: !0
	});
	return n || t(r, Symbol.toStringTag, { value: "Module" }), r;
}, c = (e, i, o, s) => {
	if (i && typeof i == "object" || typeof i == "function") for (var c = r(i), l = 0, u = c.length, d; l < u; l++) d = c[l], !a.call(e, d) && d !== o && t(e, d, {
		get: ((e) => i[e]).bind(null, d),
		enumerable: !(s = n(i, d)) || s.enumerable
	});
	return e;
}, l = (n, r, o) => (o = n == null ? {} : e(i(n)), c(r || !n || !n.__esModule || !a.call(n, "default") ? t(o, "default", {
	value: n,
	enumerable: !0
}) : o, n)), u = class extends Error {
	type;
	cause;
	constructor(e, t, n) {
		super(t), this.type = e, this.cause = n;
	}
}, d = class extends u {
	name = "PeerError";
}, f = class extends u {
	name = "PeerWarning";
}, p = class extends u {
	name = "TrackerError";
}, m = class extends u {
	name = "TrackerWarning";
}, h = class extends u {
	name = "PeerConnectError";
}, g = class extends u {
	name = "RequestError";
	timestamp;
	constructor(e, t, n) {
		super(e, t, n), this.timestamp = performance.now();
	}
}, _ = class extends u {
	name = "CoreRequestError";
}, ee = class {
	aborted = !1;
	#e = /* @__PURE__ */ new Set();
	addEventListener(e, t) {
		this.#e.add(t);
	}
	removeEventListener(e, t) {
		this.#e.delete(t);
	}
	dispatchEvent(e) {
		this.aborted = !0;
		for (let e of this.#e) try {
			e();
		} catch {}
		this.#e.clear();
	}
}, te = class {
	signal = new ee();
	abort() {
		this.signal.dispatchEvent("abort");
	}
}, ne = typeof AbortController < "u", re = ne ? AbortController : te, ie = class {
	request;
	httpConfig;
	abortController = new re();
	expectedBytesLength;
	requestByteRange;
	onChunkDownloaded;
	isAborted() {
		return this.abortController.signal.aborted;
	}
	constructor(e, t, n) {
		this.request = e, this.httpConfig = t, this.onChunkDownloaded = n.getEventDispatcher("onChunkDownloaded");
		let { byteRange: r } = this.request.segment;
		r && (this.requestByteRange = { ...r });
	}
	execute() {
		let e = {
			onAbort: () => this.abortController.abort(),
			notReceivingBytesTimeoutMs: this.httpConfig.httpNotReceivingBytesTimeoutMs
		};
		if (this.request.tryCompleteByLoadedBytes({ downloadSource: "http" }, e, this.httpConfig.validateHTTPSegment, "http-segment-validation-failed")) return;
		this.request.loadedBytes !== 0 && (this.requestByteRange = this.requestByteRange ?? { start: 0 }, this.requestByteRange.start = this.requestByteRange.start + this.request.loadedBytes), this.request.totalBytes && (this.expectedBytesLength = this.request.totalBytes - this.request.loadedBytes);
		let t = this.request.start({ downloadSource: "http" }, e);
		this.fetch(t);
	}
	async fetch(e) {
		let { segment: t } = this.request, n;
		if (this.isAborted()) return;
		let r = () => {
			try {
				n?.cancel().catch(() => {});
			} catch {}
		};
		this.abortController.signal.addEventListener("abort", r);
		let i = ne ? this.abortController.signal : void 0;
		try {
			let r = await this.httpConfig.httpRequestSetup?.(t.url, t.byteRange, i, this.requestByteRange);
			if (this.isAborted()) throw new DOMException("Request aborted", "AbortError");
			if (!r) {
				let e = new Headers();
				this.requestByteRange && e.set("Range", `bytes=${this.requestByteRange.start}-${this.requestByteRange.end ?? ""}`);
				let n = { headers: e };
				i && (n.signal = i), r = new Request(t.url, n);
			}
			if (this.isAborted()) throw new DOMException("Request aborted before request fetch", "AbortError");
			let a = await window.fetch(r);
			if (this.isAborted()) throw new DOMException("Request aborted", "AbortError");
			if (this.handleResponseHeaders(a), e.firstBytesReceived(), !a.body || typeof a.body.getReader != "function") {
				let n = await a.arrayBuffer();
				if (this.isAborted()) throw new DOMException("Request aborted", "AbortError");
				let r = new Uint8Array(n);
				e.addLoadedChunk(r), this.onChunkDownloaded(r.byteLength, "http", void 0, t.stream.type, this.request.infoHash);
			} else {
				let r = a.body.getReader();
				for (n = r;;) {
					if (this.isAborted()) throw new DOMException("Request aborted", "AbortError");
					let { done: n, value: i } = await r.read();
					if (n) break;
					if (this.isAborted()) throw new DOMException("Request aborted", "AbortError");
					e.addLoadedChunk(i), this.onChunkDownloaded(i.byteLength, "http", void 0, t.stream.type, this.request.infoHash);
				}
			}
			if (this.isAborted()) throw new DOMException("Request aborted", "AbortError");
			if (this.request.totalBytes !== void 0 && this.request.loadedBytes !== this.request.totalBytes) throw new g("http-bytes-mismatch", `HTTP response truncated: received ${this.request.loadedBytes} of ${this.request.totalBytes} bytes`);
			let o = await this.request.validateData(this.httpConfig.validateHTTPSegment);
			if (this.isAborted()) throw new DOMException("Request aborted", "AbortError");
			if (!o) throw this.request.clearLoadedBytes(), new g("http-segment-validation-failed");
			e.completeOnSuccess();
		} catch (t) {
			this.handleError(t, e);
		} finally {
			this.abortController.signal.removeEventListener("abort", r);
		}
	}
	handleResponseHeaders(e) {
		if (!e.ok) throw e.status === 406 || e.status === 416 ? (this.request.clearLoadedBytes(), new g("http-bytes-mismatch", e.statusText)) : new g("http-error", e.statusText);
		let { requestByteRange: t } = this;
		if (t) {
			if (e.status === 200) {
				if (this.request.segment.byteRange) throw new g("http-unexpected-status-code");
				this.request.clearLoadedBytes();
			} else {
				if (e.status !== 206) throw new g("http-unexpected-status-code", e.statusText);
				let n = e.headers.get("Content-Length");
				if (n && this.expectedBytesLength !== void 0 && this.expectedBytesLength !== +n) throw this.request.clearLoadedBytes(), new g("http-bytes-mismatch", e.statusText);
				let r = e.headers.get("Content-Range"), i = r ? oe(r) : void 0;
				if (i) {
					let { from: n, to: r } = i, a = r !== void 0 && n !== void 0 ? r - n + 1 : void 0;
					if (a !== void 0 && this.expectedBytesLength !== a || n !== void 0 && t.start !== n || r !== void 0 && t.end !== void 0 && t.end !== r) throw this.request.clearLoadedBytes(), new g("http-bytes-mismatch", e.statusText);
				}
			}
		}
		if (e.status === 200 && this.request.totalBytes === void 0) {
			let t = e.headers.get("Content-Length");
			t && this.request.setTotalBytes(+t);
		}
	}
	handleError(e, t) {
		if (!this.isAborted() && e instanceof Error) {
			let n = e instanceof g ? e : new g("http-error", e.message);
			t.failWithError(n);
		}
	}
}, ae = /^bytes (?:(?:(\d+)|)-(?:(\d+)|)|\*)\/(?:(\d+)|\*)$/;
function oe(e) {
	let t = ae.exec(e.trim());
	if (!t) return;
	let [, n, r, i] = t;
	return {
		from: n ? parseInt(n) : void 0,
		to: r ? parseInt(r) : void 0,
		total: i ? parseInt(i) : void 0
	};
}
//#endregion
//#region ../../node_modules/.pnpm/ms@2.1.3/node_modules/ms/index.js
var se = /* @__PURE__ */ o(((e, t) => {
	var n = 1e3, r = n * 60, i = r * 60, a = i * 24, o = a * 7, s = a * 365.25;
	t.exports = function(e, t) {
		t ||= {};
		var n = typeof e;
		if (n === "string" && e.length > 0) return c(e);
		if (n === "number" && isFinite(e)) return t.long ? u(e) : l(e);
		throw Error("val is not a non-empty string or a valid number. val=" + JSON.stringify(e));
	};
	function c(e) {
		if (e = String(e), !(e.length > 100)) {
			var t = /^(-?(?:\d+)?\.?\d+) *(milliseconds?|msecs?|ms|seconds?|secs?|s|minutes?|mins?|m|hours?|hrs?|h|days?|d|weeks?|w|years?|yrs?|y)?$/i.exec(e);
			if (t) {
				var c = parseFloat(t[1]);
				switch ((t[2] || "ms").toLowerCase()) {
					case "years":
					case "year":
					case "yrs":
					case "yr":
					case "y": return c * s;
					case "weeks":
					case "week":
					case "w": return c * o;
					case "days":
					case "day":
					case "d": return c * a;
					case "hours":
					case "hour":
					case "hrs":
					case "hr":
					case "h": return c * i;
					case "minutes":
					case "minute":
					case "mins":
					case "min":
					case "m": return c * r;
					case "seconds":
					case "second":
					case "secs":
					case "sec":
					case "s": return c * n;
					case "milliseconds":
					case "millisecond":
					case "msecs":
					case "msec":
					case "ms": return c;
					default: return;
				}
			}
		}
	}
	function l(e) {
		var t = Math.abs(e);
		return t >= a ? Math.round(e / a) + "d" : t >= i ? Math.round(e / i) + "h" : t >= r ? Math.round(e / r) + "m" : t >= n ? Math.round(e / n) + "s" : e + "ms";
	}
	function u(e) {
		var t = Math.abs(e);
		return t >= a ? d(e, t, a, "day") : t >= i ? d(e, t, i, "hour") : t >= r ? d(e, t, r, "minute") : t >= n ? d(e, t, n, "second") : e + " ms";
	}
	function d(e, t, n, r) {
		var i = t >= n * 1.5;
		return Math.round(e / n) + " " + r + (i ? "s" : "");
	}
})), ce = /* @__PURE__ */ o(((e, t) => {
	function n(e) {
		n.debug = n, n.default = n, n.coerce = c, n.disable = o, n.enable = i, n.enabled = s, n.humanize = se(), n.destroy = l, Object.keys(e).forEach((t) => {
			n[t] = e[t];
		}), n.names = [], n.skips = [], n.formatters = {};
		function t(e) {
			let t = 0;
			for (let n = 0; n < e.length; n++) t = (t << 5) - t + e.charCodeAt(n), t |= 0;
			return n.colors[Math.abs(t) % n.colors.length];
		}
		n.selectColor = t;
		function n(e) {
			let t, i = null, a, o;
			function s(...e) {
				if (!s.enabled) return;
				let r = s, i = Number(/* @__PURE__ */ new Date());
				r.diff = i - (t || i), r.prev = t, r.curr = i, t = i, e[0] = n.coerce(e[0]), typeof e[0] != "string" && e.unshift("%O");
				let a = 0;
				e[0] = e[0].replace(/%([a-zA-Z%])/g, (t, i) => {
					if (t === "%%") return "%";
					a++;
					let o = n.formatters[i];
					if (typeof o == "function") {
						let n = e[a];
						t = o.call(r, n), e.splice(a, 1), a--;
					}
					return t;
				}), n.formatArgs.call(r, e), (r.log || n.log).apply(r, e);
			}
			return s.namespace = e, s.useColors = n.useColors(), s.color = n.selectColor(e), s.extend = r, s.destroy = n.destroy, Object.defineProperty(s, "enabled", {
				enumerable: !0,
				configurable: !1,
				get: () => i === null ? (a !== n.namespaces && (a = n.namespaces, o = n.enabled(e)), o) : i,
				set: (e) => {
					i = e;
				}
			}), typeof n.init == "function" && n.init(s), s;
		}
		function r(e, t) {
			let r = n(this.namespace + (t === void 0 ? ":" : t) + e);
			return r.log = this.log, r;
		}
		function i(e) {
			n.save(e), n.namespaces = e, n.names = [], n.skips = [];
			let t = (typeof e == "string" ? e : "").trim().replace(/\s+/g, ",").split(",").filter(Boolean);
			for (let e of t) e[0] === "-" ? n.skips.push(e.slice(1)) : n.names.push(e);
		}
		function a(e, t) {
			let n = 0, r = 0, i = -1, a = 0;
			for (; n < e.length;) if (r < t.length && (t[r] === e[n] || t[r] === "*")) t[r] === "*" ? (i = r, a = n, r++) : (n++, r++);
			else if (i !== -1) r = i + 1, a++, n = a;
			else return !1;
			for (; r < t.length && t[r] === "*";) r++;
			return r === t.length;
		}
		function o() {
			let e = [...n.names, ...n.skips.map((e) => "-" + e)].join(",");
			return n.enable(""), e;
		}
		function s(e) {
			for (let t of n.skips) if (a(e, t)) return !1;
			for (let t of n.names) if (a(e, t)) return !0;
			return !1;
		}
		function c(e) {
			return e instanceof Error ? e.stack || e.message : e;
		}
		function l() {
			console.warn("Instance method `debug.destroy()` is deprecated and no longer does anything. It will be removed in the next major version of `debug`.");
		}
		return n.enable(n.load()), n;
	}
	t.exports = n;
})), v = /* @__PURE__ */ l((/* @__PURE__ */ o(((e, t) => {
	e.formatArgs = r, e.save = i, e.load = a, e.useColors = n, e.storage = o(), e.destroy = (() => {
		let e = !1;
		return () => {
			e || (e = !0, console.warn("Instance method `debug.destroy()` is deprecated and no longer does anything. It will be removed in the next major version of `debug`."));
		};
	})(), e.colors = /* @__PURE__ */ "#0000CC.#0000FF.#0033CC.#0033FF.#0066CC.#0066FF.#0099CC.#0099FF.#00CC00.#00CC33.#00CC66.#00CC99.#00CCCC.#00CCFF.#3300CC.#3300FF.#3333CC.#3333FF.#3366CC.#3366FF.#3399CC.#3399FF.#33CC00.#33CC33.#33CC66.#33CC99.#33CCCC.#33CCFF.#6600CC.#6600FF.#6633CC.#6633FF.#66CC00.#66CC33.#9900CC.#9900FF.#9933CC.#9933FF.#99CC00.#99CC33.#CC0000.#CC0033.#CC0066.#CC0099.#CC00CC.#CC00FF.#CC3300.#CC3333.#CC3366.#CC3399.#CC33CC.#CC33FF.#CC6600.#CC6633.#CC9900.#CC9933.#CCCC00.#CCCC33.#FF0000.#FF0033.#FF0066.#FF0099.#FF00CC.#FF00FF.#FF3300.#FF3333.#FF3366.#FF3399.#FF33CC.#FF33FF.#FF6600.#FF6633.#FF9900.#FF9933.#FFCC00.#FFCC33".split(".");
	function n() {
		if (typeof window < "u" && window.process && (window.process.type === "renderer" || window.process.__nwjs)) return !0;
		if (typeof navigator < "u" && navigator.userAgent && navigator.userAgent.toLowerCase().match(/(edge|trident)\/(\d+)/)) return !1;
		let e;
		return typeof document < "u" && document.documentElement && document.documentElement.style && document.documentElement.style.WebkitAppearance || typeof window < "u" && window.console && (window.console.firebug || window.console.exception && window.console.table) || typeof navigator < "u" && navigator.userAgent && (e = navigator.userAgent.toLowerCase().match(/firefox\/(\d+)/)) && parseInt(e[1], 10) >= 31 || typeof navigator < "u" && navigator.userAgent && navigator.userAgent.toLowerCase().match(/applewebkit\/(\d+)/);
	}
	function r(e) {
		if (e[0] = (this.useColors ? "%c" : "") + this.namespace + (this.useColors ? " %c" : " ") + e[0] + (this.useColors ? "%c " : " ") + "+" + t.exports.humanize(this.diff), !this.useColors) return;
		let n = "color: " + this.color;
		e.splice(1, 0, n, "color: inherit");
		let r = 0, i = 0;
		e[0].replace(/%[a-zA-Z%]/g, (e) => {
			e !== "%%" && (r++, e === "%c" && (i = r));
		}), e.splice(i, 0, n);
	}
	e.log = console.debug || console.log || (() => {});
	function i(t) {
		try {
			t ? e.storage.setItem("debug", t) : e.storage.removeItem("debug");
		} catch {}
	}
	function a() {
		let t;
		try {
			t = e.storage.getItem("debug") || e.storage.getItem("DEBUG");
		} catch {}
		return !t && typeof process < "u" && "env" in process && (t = process.env.DEBUG), t;
	}
	function o() {
		try {
			return localStorage;
		} catch {}
	}
	t.exports = ce()(e);
	var { formatters: s } = t.exports;
	s.j = function(e) {
		try {
			return JSON.stringify(e);
		} catch (e) {
			return "[UnexpectedJSONParseError]: " + e.message;
		}
	};
})))(), 1), y = /* @__PURE__ */ function(e) {
	return e[e.SegmentsAnnouncement = 0] = "SegmentsAnnouncement", e[e.SegmentRequest = 1] = "SegmentRequest", e[e.SegmentData = 2] = "SegmentData", e[e.SegmentDataSendingCompleted = 3] = "SegmentDataSendingCompleted", e[e.SegmentAbsent = 4] = "SegmentAbsent", e[e.CancelSegmentRequest = 5] = "CancelSegmentRequest", e;
}({});
//#endregion
//#region src/utils/utils.ts
function le() {
	let e, t;
	return {
		promise: new Promise((n, r) => {
			e = n, t = r;
		}),
		resolve: e,
		reject: t
	};
}
function ue(e) {
	Promise.resolve().then(e);
}
function de(e, t) {
	t ??= e.reduce((e, t) => e + t.byteLength, 0);
	let n = new Uint8Array(t), r = 0;
	for (let t of e) n.set(t, r), r += t.byteLength;
	return n;
}
function fe(e) {
	return e[Math.floor(Math.random() * e.length)];
}
function pe(e, t) {
	if (e.length === 0) throw Error("Cannot get item from empty array");
	if (e.length === 1) return e[0];
	let n = 0, r = e.map((e) => {
		let r = t(e);
		return n += r, r;
	}), i = Math.random() * n;
	for (let t = 0; t < e.length; t++) if (i -= r[t], i <= 0) return e[t];
	return e[e.length - 1];
}
function me(e) {
	return new TextEncoder().encode(e);
}
function* he(e) {
	for (let t = e.length - 1; t >= 0; t--) yield e[t];
}
function ge(e) {
	return !!e && typeof e == "object" && !Array.isArray(e);
}
function _e(e) {
	return Array.isArray(e);
}
function ve(e) {
	function t(e) {
		if (ge(e)) {
			let n = {};
			return Object.keys(e).forEach((r) => {
				if (e[r] !== void 0) {
					let i = t(e[r]);
					i !== void 0 && (n[r] = i);
				}
			}), n;
		}
		return e;
	}
	return t(e);
}
function b(e) {
	if (_e(e)) return e.map((e) => b(e));
	if (ge(e)) {
		let t = {};
		for (let n of Object.keys(e)) t[n] = b(e[n]);
		return t;
	}
	return e;
}
function ye(e) {
	for (let t = e.length - 1; t > 0; t--) {
		let n = Math.floor(Math.random() * (t + 1));
		[e[t], e[n]] = [e[n], e[t]];
	}
	return e;
}
function x(e, t, n = {}) {
	return typeof e != "object" || !e || typeof t != "object" || !t || Object.keys(t).forEach((r) => {
		let i = typeof r == "symbol" ? r.toString() : String(r);
		if (r === "__proto__" || r === "constructor" || r === "prototype") throw Error(`Attempt to modify restricted property '${i}'`);
		let a = t[r], o = n[r];
		r in e && (e[r] = a === void 0 ? o === void 0 ? void 0 : o : a);
	}), e;
}
function S(e) {
	let { defaultConfig: t, baseConfig: n = {}, specificStreamConfig: r = {} } = e, i = b({
		...t,
		...n,
		...r
	}), a = Object.keys(t), o = {};
	return a.forEach((e) => {
		e in i && (o[e] = i[e]);
	}), o;
}
//#endregion
//#region src/p2p/commands/binary-serialization.ts
var be = new TextEncoder(), xe = new TextDecoder("utf8"), C = /* @__PURE__ */ function(e) {
	return e[e.Min = -1] = "Min", e[e.Int = 0] = "Int", e[e.SimilarIntArray = 1] = "SimilarIntArray", e[e.String = 2] = "String", e[e.Max = 3] = "Max", e;
}({});
function Se(e) {
	if (e === 0) return 1;
	let t = Math.floor(Math.log2(Math.abs(e))) + 2;
	return Math.ceil(t / 8);
}
function Ce(e) {
	let t = e < 0, n = Se(e), r = new Uint8Array(n);
	e = Math.abs(e);
	for (let t = 0; t < n; t++) {
		let i = 8 * (n - 1 - t), a = Math.floor(e / 2 ** i) & 255;
		r[t] = a;
	}
	return t && (r[0] |= 128), r;
}
function we(e) {
	let t = e.length, n = (e, n) => e * 2 ** (8 * (t - 1 - n)), r = n(e[0] & 127, 0);
	for (let i = 1; i < t; i++) r += n(e[i], i);
	return (e[0] & 128) >> 7 && (r = -r), r;
}
function Te(e) {
	let t = Ce(e), n = 0 | t.length;
	return new Uint8Array([n, ...t]);
}
function Ee(e) {
	if (e.length === 0) throw Error("Buffer is too short");
	let t = e[0];
	if (t >> 4) throw Error("Trying to deserialize integer with invalid serialized item code");
	let n = t & 15;
	if (n === 0) throw Error("Invalid integer: zero byte length");
	if (n > 7) throw Error("Invalid integer: byte length exceeds safe integer limit");
	let r = 1 + n;
	if (e.length < r) throw Error("Buffer is too short");
	return {
		number: we(e.subarray(1, r)),
		byteLength: n + 1
	};
}
function De(e) {
	let t = /* @__PURE__ */ new Map();
	for (let n of e) {
		let e = n & 255, r = n - e, i = t.get(r) ?? new w();
		i.length || t.set(r, i), i.push(e);
	}
	let n = new w();
	n.push([16, t.size]);
	for (let [e, r] of t) {
		let { length: t } = r, i = e + (t & 255);
		r.unshift(Te(i)), n.push(r.getBuffer());
	}
	return n.getBuffer();
}
function Oe(e) {
	if (e.length < 2) throw Error("Buffer is too short");
	let [t, n] = e;
	if (t >> 4 != 1) throw Error("Trying to deserialize similar int array with invalid serialized item code");
	let r = 2, i = [];
	for (let t = 0; t < n; t++) {
		let { number: t, byteLength: n } = Ee(e.subarray(r));
		r += n;
		let a = t & 255, o = a === 0 ? 256 : a, s = t - a;
		if (r + o > e.length) throw Error("Malformed similar int array: buffer too short");
		for (let t = 0; t < o; t++) {
			let t = e[r];
			i.push(s + t), r++;
		}
	}
	return {
		numbers: i,
		byteLength: r
	};
}
function ke(e) {
	let t = be.encode(e), { length: n } = t;
	if (n > 4095) throw Error("String exceeds maximum length of 4095 bytes");
	let r = new w();
	return r.push([32 | n >> 8 & 15, n & 255]), r.push(t), r.getBuffer();
}
function Ae(e) {
	if (e.length < 2) throw Error("Buffer is too short");
	let [t, n] = e;
	if (t >> 4 != 2) throw Error("Trying to deserialize bytes (sting) with invalid serialized item code.");
	let r = (t & 15) << 8 | n;
	if (e.length < r + 2) throw Error("Malformed string: buffer too short");
	let i = e.subarray(2, r + 2);
	return {
		string: xe.decode(i),
		byteLength: r + 2
	};
}
var w = class {
	#e = [];
	#t = 0;
	push(e) {
		this.#n(e, "end");
	}
	unshift(e) {
		this.#n(e, "start");
	}
	#n(e, t) {
		let n;
		n = e instanceof Uint8Array ? e : Array.isArray(e) ? new Uint8Array(e) : new Uint8Array([e]), this.#t += n.length, this.#e[t === "start" ? "unshift" : "push"](n);
	}
	getBytesChunks() {
		return this.#e;
	}
	getBuffer() {
		return de(this.#e, this.#t);
	}
	get length() {
		return this.#t;
	}
}, T = 4, E = N("cstr", T), D = N("cend", T), O = N("dstr", T), k = N("dend", T), je = [E, O], Me = [D, k], A = E.length + D.length;
function Ne(e) {
	if (e.length < A) return !1;
	let { length: t } = E, n = e.subarray(-t);
	return je.some((t) => F(e, t, T)) && Me.some((e) => F(n, e, T));
}
function Pe(e) {
	return e.length < A ? !1 : F(e, E, T);
}
function Fe(e) {
	return e.length < A ? !1 : F(e.subarray(-4), D, T);
}
var j = class extends Error {
	type;
	constructor(e) {
		super(), this.type = e;
	}
}, Ie = class {
	#e = new w();
	#t = "joining";
	#n;
	constructor(e) {
		this.#n = e;
	}
	addCommandChunk(e) {
		if (this.#t === "completed") return;
		let t = Pe(e);
		if (!this.#e.length && !t) throw new j("no-first-chunk");
		if (this.#e.length && t) throw new j("incomplete-joining");
		this.#e.push(this.#r(e)), Fe(e) && (this.#t = "completed", this.#n(this.#e.getBuffer()));
	}
	#r(e) {
		if (e.length < A) throw Error("Command chunk is too short to unframe");
		return e.subarray(T, e.length - T);
	}
}, M = class {
	#e = new w();
	#t = [];
	#n = "creating";
	#r;
	constructor(e, t) {
		this.#r = t, this.#e.push(e);
	}
	addInteger(e, t) {
		this.#e.push(e.charCodeAt(0));
		let n = Te(t);
		this.#e.push(n);
	}
	addUniqueSimilarIntArr(e, t) {
		this.#e.push(e.charCodeAt(0));
		let n = De(t);
		this.#e.push(n);
	}
	addString(e, t) {
		this.#e.push(e.charCodeAt(0));
		let n = ke(t);
		this.#e.push(n);
	}
	complete() {
		if (!this.#e.length) throw Error("Buffer is empty");
		if (this.#n === "completed") return;
		this.#n = "completed";
		let e = this.#e.getBuffer();
		if (e.length + A <= this.#r) {
			this.#t.push(P(e, E, D));
			return;
		}
		let t = Math.ceil(e.length / this.#r);
		Math.ceil(e.length / t) + A > this.#r && t++;
		for (let [n, r] of ze(e, t)) n === 0 ? this.#t.push(P(r, E, k)) : n === t - 1 ? this.#t.push(P(r, O, D)) : this.#t.push(P(r, O, k));
	}
	getResultBuffers() {
		if (this.#n === "creating" || !this.#t.length) throw Error("Command is not complete.");
		return this.#t;
	}
};
function Le(e) {
	let [t] = e, n = { c: t }, r = 1;
	for (; r < e.length;) {
		if (r + 1 >= e.length) throw Error("Malformed command buffer: truncated name/type header");
		let t = String.fromCharCode(e[r]);
		switch (r++, Re(e[r])) {
			case C.Int:
				{
					let { number: i, byteLength: a } = Ee(e.subarray(r));
					n[t] = i, r += a;
				}
				break;
			case C.SimilarIntArray:
				{
					let { numbers: i, byteLength: a } = Oe(e.subarray(r));
					n[t] = i, r += a;
				}
				break;
			case C.String: {
				let { string: i, byteLength: a } = Ae(e.subarray(r));
				n[t] = i, r += a;
			}
		}
	}
	return Be(n);
}
function Re(e) {
	let t = e >> 4;
	if (t <= C.Min || t >= C.Max) throw Error("Not existing type");
	return t;
}
function N(e, t) {
	if (t && e.length !== t) throw Error("Wrong string length");
	let n = new Uint8Array(t ?? e.length);
	for (let t = 0; t < e.length; t++) n[t] = e.charCodeAt(t);
	return n;
}
function* ze(e, t) {
	let n = Math.ceil(e.length / t);
	for (let r = 0; r < t; r++) yield [r, e.subarray(r * n, (r + 1) * n)];
}
function P(e, t, n) {
	let r = new Uint8Array(e.length + t.length + n.length);
	return r.set(t), r.set(e, t.length), r.set(n, t.length + e.length), r;
}
function F(e, t, n) {
	for (let r = 0; r < n; r++) if (e[r] !== t[r]) return !1;
	return !0;
}
function Be(e) {
	switch (e.c) {
		case y.SegmentsAnnouncement: return e;
		case y.SegmentRequest: return I(e, "i", "r"), e;
		case y.SegmentData: return I(e, "i", "r", "s"), e;
		case y.SegmentAbsent:
		case y.CancelSegmentRequest:
		case y.SegmentDataSendingCompleted: return I(e, "i", "r"), e;
		default: throw Error(`Unknown peer command type: ${String(e.c)}`);
	}
}
function I(e, ...t) {
	for (let n of t) if (typeof e[n] != "number") throw Error(`Expected number field "${n}", got ${typeof e[n]}`);
}
//#endregion
//#region src/p2p/commands/commands.ts
function Ve(e, t) {
	let { c: n, p: r, l: i } = e, a = new M(n, t);
	return i?.length && a.addUniqueSimilarIntArr("l", i), r?.length && a.addUniqueSimilarIntArr("p", r), a.complete(), a.getResultBuffers();
}
function He(e, t) {
	let n = new M(e.c, t);
	return n.addInteger("i", e.i), n.addInteger("r", e.r), n.complete(), n.getResultBuffers();
}
function Ue(e, t) {
	let n = new M(e.c, t);
	return n.addInteger("i", e.i), n.addInteger("s", e.s), n.addInteger("r", e.r), n.complete(), n.getResultBuffers();
}
function We(e, t) {
	let n = new M(e.c, t);
	return n.addInteger("i", e.i), n.addInteger("r", e.r), e.b && n.addInteger("b", e.b), n.complete(), n.getResultBuffers();
}
function Ge(e, t) {
	switch (e.c) {
		case y.CancelSegmentRequest:
		case y.SegmentAbsent:
		case y.SegmentDataSendingCompleted: return He(e, t);
		case y.SegmentRequest: return We(e, t);
		case y.SegmentsAnnouncement: return Ve(e, t);
		case y.SegmentData: return Ue(e, t);
	}
}
//#endregion
//#region src/p2p/commands/index.ts
var Ke = /* @__PURE__ */ s({
	BinaryCommandChunksJoiner: () => Ie,
	BinaryCommandJoiningError: () => j,
	PeerCommandType: () => y,
	deserializeCommand: () => Le,
	isCommandChunk: () => Ne,
	serializePeerCommand: () => Ge
});
//#endregion
//#region src/webtorrent/utils.ts
function qe(e, t = "RTC error") {
	let n = e;
	if (n.error instanceof Error) return n.error;
	let r = n.error?.message ?? t;
	return Error(r);
}
function Je(e, t = "RTC error") {
	return qe(e, t).message;
}
function L(e) {
	return e === "failed" || e === "closed" || e === "disconnected";
}
//#endregion
//#region src/webtorrent/data-channel-sender.ts
var Ye = 65536, Xe = class {
	channel;
	maxMessageSize;
	#e;
	constructor(e, t) {
		this.channel = e, this.maxMessageSize = t;
	}
	async sendData(e, t) {
		if (this.#e) throw Error("Already sending data");
		if (this.channel.readyState !== "open") throw Error("Data channel is not open");
		this.channel.bufferedAmountLowThreshold = Ye;
		let { promise: n, resolve: r, reject: i } = le(), a = 0, o = !1, s = () => !o && (o = !0, this.#e = void 0, this.channel.removeEventListener("bufferedamountlow", f), this.channel.removeEventListener("closing", c), this.channel.removeEventListener("close", c), this.channel.removeEventListener("error", l), !0);
		this.#e = { cancel: () => {
			s() && i(/* @__PURE__ */ Error("Send cancelled"));
		} };
		let c = () => {
			s() && i(/* @__PURE__ */ Error("Data channel closed"));
		}, l = (e) => {
			if (!s()) return;
			let t = Je(e, "Unknown error");
			i(/* @__PURE__ */ Error(`Data channel error: ${t}`));
		}, u = ArrayBuffer.isView(e) ? e.buffer : e, d = ArrayBuffer.isView(e) ? e.byteOffset : 0, f = () => {
			if (!o) {
				if (this.channel.readyState !== "open") {
					s() && i(/* @__PURE__ */ Error(`Data channel not open (state: ${this.channel.readyState})`));
					return;
				}
				try {
					for (; a < e.byteLength;) {
						if (this.channel.bufferedAmount > Ye) return;
						let n = Math.min(this.maxMessageSize, e.byteLength - a), r = new Uint8Array(u, d + a, n);
						if (this.channel.send(r), a += n, t?.(n), !this.#e) return;
					}
				} catch (e) {
					s() && i(e instanceof Error ? e : Error(String(e)));
					return;
				}
				s() && r();
			}
		};
		return this.channel.addEventListener("bufferedamountlow", f), this.channel.addEventListener("closing", c), this.channel.addEventListener("close", c), this.channel.addEventListener("error", l), f(), n;
	}
	cancel() {
		this.#e?.cancel();
	}
}, R = (0, v.default)("p2pml-core:peer-protocol"), Ze = class {
	#e;
	#t;
	#n;
	#r;
	#i;
	#a;
	#o;
	#s;
	#c;
	constructor(e, t, n, r, i) {
		if (this.#a = e, this.#o = t, this.#s = n, this.#c = i, this.#t = new Xe(e, t.webRtcMaxMessageSize), this.#r = r.getEventDispatcher("onChunkDownloaded"), this.#i = r.getEventDispatcher("onChunkUploaded"), e.binaryType !== "arraybuffer") throw Error(`Expected binaryType "arraybuffer", got "${e.binaryType}"`);
		e.addEventListener("message", this.#l);
	}
	#l = (e) => {
		try {
			let t = new Uint8Array(e.data);
			Ne(t) ? this.#u(t) : (this.#s.onSegmentChunkReceived(t), this.#r(t.byteLength, "p2p", this.#c, this.#o.streamType, this.#o.infoHash));
		} catch (e) {
			R("error handling data channel message: %O", e), this.#s.onProtocolError(e);
		}
	};
	sendCommand(e) {
		if (this.#a.readyState !== "open") throw Error(`cannot send command ${e.c} (channel state: ${this.#a.readyState})`);
		let t = Ge(e, this.#o.webRtcMaxMessageSize);
		for (let e of t) this.#a.send(e);
	}
	stopUploadingSegmentData() {
		this.#t.cancel(), this.#n = void 0;
	}
	getUploadingRequestId() {
		return this.#n;
	}
	async splitSegmentDataToChunksAndUploadAsync(e, t) {
		if (this.#n !== void 0) throw Error("Some segment data is already uploading.");
		this.#n = t;
		try {
			await this.#t.sendData(e, (e) => {
				this.#i(e, this.#c, this.#o.streamType, this.#o.infoHash);
			});
		} finally {
			this.#n === t && (this.#n = void 0);
		}
	}
	#u(e) {
		this.#e ??= new Ie((e) => {
			this.#e = void 0;
			let t;
			try {
				t = Le(e);
			} catch (e) {
				R("error deserializing command: %O", e), this.#s.onProtocolError(e);
				return;
			}
			this.#s.onCommandReceived(t);
		});
		try {
			this.#e.addCommandChunk(e);
		} catch (e) {
			R("error receiving command chunks: %O", e), this.#e = void 0, this.#s.onProtocolError(e);
		}
	}
	destroy() {
		this.#a.removeEventListener("message", this.#l), this.#t.cancel(), this.#e = void 0, this.#n = void 0;
	}
}, Qe = 1, z = class {
	clearThresholdMs;
	loadingsCount = 0;
	bytes = [];
	loadingOnlyTimestamps = [];
	timestamps = [];
	noLoadingsTime = 0;
	loadingsStoppedAt = 0;
	constructor(e = 2e4) {
		this.clearThresholdMs = e;
	}
	addBytes(e, t = performance.now()) {
		this.bytes.push(e), this.loadingOnlyTimestamps.push(t - this.noLoadingsTime), this.timestamps.push(t);
	}
	startLoading(e = performance.now()) {
		this.clearStale(), this.loadingsCount === 0 && this.loadingsStoppedAt !== 0 && (this.noLoadingsTime += e - this.loadingsStoppedAt), this.loadingsCount++;
	}
	stopLoading(e = performance.now()) {
		this.loadingsCount > 0 && (this.loadingsCount--, this.loadingsCount === 0 && (this.loadingsStoppedAt = e));
	}
	getBandwidthLoadingOnly(e, t = -Infinity) {
		if (!this.loadingOnlyTimestamps.length) return 0;
		let n = e * 1e3, r = this.loadingOnlyTimestamps[this.loadingOnlyTimestamps.length - 1], i = r, a = r - n, o = 0;
		for (let e = this.bytes.length - 1; e >= 0; e--) {
			let n = this.loadingOnlyTimestamps[e];
			if (n < a || this.timestamps[e] < t) break;
			i = n, o += this.bytes[e];
		}
		let s = Math.max(r - i, Qe);
		return o * 8e3 / s;
	}
	getBandwidth(e, t = -Infinity, n = performance.now()) {
		if (!this.timestamps.length) return 0;
		let r = n - e * 1e3, i = n, a = 0;
		for (let e = this.bytes.length - 1; e >= 0; e--) {
			let n = this.timestamps[e];
			if (n < r || n < t) break;
			i = n, a += this.bytes[e];
		}
		let o = Math.max(n - i, Qe);
		return a * 8e3 / o;
	}
	clearStale() {
		if (!this.loadingOnlyTimestamps.length) return;
		let e = this.loadingOnlyTimestamps[this.loadingOnlyTimestamps.length - 1] - this.clearThresholdMs, t = 0;
		for (let n of this.loadingOnlyTimestamps) {
			if (n > e) break;
			t++;
		}
		this.bytes.splice(0, t), this.loadingOnlyTimestamps.splice(0, t), this.timestamps.splice(0, t);
	}
	clear() {
		this.bytes.length = 0, this.loadingOnlyTimestamps.length = 0, this.timestamps.length = 0, this.loadingsCount = 0, this.noLoadingsTime = 0, this.loadingsStoppedAt = 0;
	}
}, { PeerCommandType: B } = Ke, $e = class {
	id;
	channel;
	eventTarget;
	#e;
	#t;
	#n = /* @__PURE__ */ new Set();
	#r = /* @__PURE__ */ new Set();
	#i = 0;
	#a = new z();
	#o = {
		value: 0,
		timestamp: 0
	};
	#s = (0, v.default)("p2pml-core:peer");
	#c = 0;
	#l;
	#u = !1;
	connectedAt = performance.now();
	#d;
	#f;
	#p;
	get isDestroyed() {
		return this.#u;
	}
	constructor(e, t, n, r, i, a) {
		this.id = e, this.channel = t, this.eventTarget = a, this.#d = n, this.#f = r, this.#p = i, this.#e = new Ze(t, i, {
			onSegmentChunkReceived: this.#h,
			onCommandReceived: (e) => void this.#m(e).catch((t) => {
				this.#s("error processing command %O: %O", e, t), this.destroy(!1, new d("protocol-violation", t instanceof Error ? t.message : "Error processing command", t));
			}),
			onProtocolError: (e) => {
				this.destroy(!1, new d("protocol-violation", e instanceof Error ? e.message : "Protocol error", e));
			}
		}, a, e);
	}
	get downloadingSegment() {
		return this.#t?.request.segment;
	}
	get isUploadingSegment() {
		return this.#e.getUploadingRequestId() !== void 0;
	}
	getDownloadBandwidth() {
		let e = performance.now();
		return e - this.#o.timestamp > 1e3 && (this.#o.value = this.#a.getBandwidthLoadingOnly(15), this.#o.timestamp = e), this.#o.value;
	}
	getSegmentStatus(e) {
		let { externalId: t } = e;
		if (this.#n.has(t)) return "loaded";
		if (this.#r.has(t)) return "http-loading";
	}
	#m = async (e) => {
		switch (e.c) {
			case B.SegmentsAnnouncement:
				this.#n = new Set(e.l), this.#r = new Set(e.p), this.#f.onSegmentsAnnouncement();
				break;
			case B.SegmentRequest:
				this.#l = e.r, this.#e.stopUploadingSegmentData(), this.#f.onSegmentRequested(this, e.i, e.r, e.b);
				break;
			case B.SegmentData:
				{
					if (!this.#t || this.#t.isSegmentDataCommandReceived) break;
					let { request: t, controls: n, requestId: r } = this.#t;
					if (t.segment.externalId !== e.i || r !== e.r) break;
					this.#t.isSegmentDataCommandReceived = !0, n.firstBytesReceived(), t.totalBytes === void 0 ? t.setTotalBytes(t.loadedBytes + e.s) : t.totalBytes - t.loadedBytes !== e.s && this.#g("bytes-length-mismatch", "Peer response bytes length mismatch");
				}
				break;
			case B.SegmentDataSendingCompleted: {
				let t = this.#t;
				if (!t?.isSegmentDataCommandReceived) return;
				let { request: n, controls: r, requestId: i } = t;
				if (n.segment.externalId !== e.i || i !== e.r) {
					this.#g("protocol-violation", "Peer protocol violation");
					return;
				}
				if (n.loadedBytes !== n.totalBytes) {
					this.#g("bytes-length-mismatch", "Peer response bytes length mismatch");
					return;
				}
				let a = await n.validateData(this.#p.validateP2PSegment);
				if (this.#u || this.#t !== t) return;
				if (!a) {
					this.#g("validation-failed", "P2P segment validation failed");
					return;
				}
				this.#i = 0, r.completeOnSuccess(), this.#a.stopLoading(), this.#t = void 0;
				break;
			}
			case B.SegmentAbsent:
				this.#n.delete(e.i), this.#t?.request.segment.externalId === e.i && this.#t.requestId === e.r && this.#_("peer-segment-absent");
				break;
			case B.CancelSegmentRequest:
				if (this.#l === e.r && (this.#l = void 0), this.#e.getUploadingRequestId() !== e.r) break;
				this.#e.stopUploadingSegmentData();
		}
	};
	#h = (e) => {
		if (!this.#t?.isSegmentDataCommandReceived) return;
		let { request: t, controls: n } = this.#t;
		if (t.totalBytes !== void 0 && t.loadedBytes + e.byteLength > t.totalBytes) {
			this.#g("bytes-length-mismatch", "Peer response bytes length mismatch");
			return;
		}
		this.#a.addBytes(e.byteLength), this.#o.timestamp = 0, n.addLoadedChunk(e);
	};
	downloadSegment(e) {
		if (this.#u) return;
		if (this.#t) throw Error("Some segment already is downloading");
		if (e.tryCompleteByLoadedBytes({
			downloadSource: "p2p",
			peerId: this.id
		}, {
			notReceivingBytesTimeoutMs: this.#p.p2pNotReceivingBytesTimeoutMs,
			onAbort: () => void 0
		}, this.#p.validateP2PSegment, "p2p-segment-validation-failed")) return;
		this.#a.startLoading(), this.#c = (this.#c + 1) % 1e9, this.#t = {
			request: e,
			requestId: this.#c,
			isSegmentDataCommandReceived: !1,
			controls: e.start({
				downloadSource: "p2p",
				peerId: this.id
			}, {
				notReceivingBytesTimeoutMs: this.#p.p2pNotReceivingBytesTimeoutMs,
				onAbort: (t) => {
					if (!this.#t || this.#t.request !== e) return;
					let { request: n, requestId: r } = this.#t;
					this.#v(n.segment, r), this.#a.stopLoading(), t.type !== "abort" && (this.#a.clear(), this.#o.timestamp = 0, this.#s(`cleared bandwidth history due to ${t.type}`)), this.#t = void 0, t.type === "bytes-receiving-timeout" && this.#i++, this.#i >= this.#p.p2pErrorRetries ? this.destroy(!1, new d("timeout", "Too many timeout errors")) : t.type === "bytes-receiving-timeout" && this.#f.onWarning(new f("timeout-strike", `Timeout strike ${this.#i}/${this.#p.p2pErrorRetries}`));
				}
			})
		};
		let t = {
			c: B.SegmentRequest,
			r: this.#t.requestId,
			i: e.segment.externalId
		};
		e.loadedBytes && (t.b = e.loadedBytes), this.#b(t) || this.#_("peer-closed");
	}
	async uploadSegmentData(e, t, n) {
		if (this.#u) return;
		if (t !== this.#l) {
			this.#s(`discarding obsolete upload request ${t} for segment ${e.externalId}`);
			return;
		}
		let { externalId: r } = e;
		this.#s(`send segment ${e.externalId} to ${this.id} (byteLength: ${n.byteLength})`);
		let i = {
			c: B.SegmentData,
			i: r,
			r: t,
			s: n.byteLength
		};
		if (this.#b(i)) try {
			if (await this.#e.splitSegmentDataToChunksAndUploadAsync(n, t), this.isDestroyed || t !== this.#l) return;
			this.#y(e, t), this.#s(`segment ${r} has been sent to ${this.id}`);
		} catch (e) {
			this.#s(`cancel segment uploading ${r}: %O`, e);
		}
	}
	#g(e, t) {
		this.#t?.request.clearLoadedBytes();
		let n = new d(e, t);
		this.#_("peer-closed", n), this.destroy(!1, n);
	}
	#_(e, t) {
		if (!this.#t) return;
		let { request: n, controls: r } = this.#t, { segment: i } = n;
		this.#s(`cancel segment request ${i.externalId} (${e})`);
		let a = new g(e, void 0, t);
		r.failWithError(a), this.#a.stopLoading(), e !== "peer-segment-absent" && (this.#a.clear(), this.#o.timestamp = 0, this.#s(`cleared bandwidth history due to ${a.type}`)), this.#t = void 0;
	}
	sendSegmentsAnnouncementCommand(e, t) {
		let n = {
			c: B.SegmentsAnnouncement,
			p: t,
			l: e
		};
		this.#b(n);
	}
	sendSegmentAbsentCommand(e, t) {
		this.#b({
			c: B.SegmentAbsent,
			i: e,
			r: t
		});
	}
	#v(e, t) {
		this.#b({
			c: B.CancelSegmentRequest,
			i: e.externalId,
			r: t
		});
	}
	#y(e, t) {
		this.#b({
			c: B.SegmentDataSendingCompleted,
			r: t,
			i: e.externalId
		});
	}
	destroy(e = !1, t) {
		this.#u || (this.#u = !0, this.#_("peer-closed", t), this.#e.destroy(), e || this.#d(t), this.#s(`peer closed ${this.id}`));
	}
	#b(e) {
		if (this.#u) return !1;
		try {
			return this.#e.sendCommand(e), !0;
		} catch (t) {
			return this.#s("error sending command %d: %O", e.c, t), !1;
		}
	}
}, V = class {
	events = /* @__PURE__ */ new Map();
	dispatchEvent(e, t, n, r, i, a) {
		let o = this.events.get(e);
		if (o) for (let e of o) try {
			e(t, n, r, i, a);
		} catch {}
	}
	getEventDispatcher(e) {
		return (t, n, r, i, a) => {
			let o = this.events.get(e);
			if (o) for (let e of o) try {
				e(t, n, r, i, a);
			} catch {}
		};
	}
	addEventListener(e, t) {
		let n = this.events.get(e);
		n ? this.events.set(e, [...n, t]) : this.events.set(e, [t]);
	}
	removeEventListener(e, t) {
		let n = this.events.get(e);
		if (!n) return;
		let r = n.indexOf(t);
		if (r === -1) return;
		if (n.length === 1) {
			this.events.delete(e);
			return;
		}
		let i = n.slice();
		i.splice(r, 1), this.events.set(e, i);
	}
	clear() {
		this.events.clear();
	}
}, H = typeof window < "u" ? window : {}, U = H.RTCPeerConnection ?? H.webkitRTCPeerConnection ?? H.mozRTCPeerConnection, et = H.RTCSessionDescription ?? H.webkitRTCSessionDescription ?? H.mozRTCSessionDescription, W = (() => {
	if (!H.RTCPeerConnection && !H.webkitRTCPeerConnection && !H.mozRTCPeerConnection) return !1;
	let e;
	try {
		e = new U();
		let t = e.createOffer();
		if (typeof t?.then == "function") return t.catch(() => {}), !0;
	} catch {} finally {
		try {
			e?.close();
		} catch {}
	}
	return !1;
})();
function tt(e, t) {
	return W ? e.createOffer(t) : new Promise((n, r) => {
		try {
			e.createOffer(n, r, t);
		} catch (e) {
			r(e);
		}
	});
}
function nt(e, t) {
	return W ? e.createAnswer(t) : new Promise((n, r) => {
		try {
			e.createAnswer(n, r, t);
		} catch (e) {
			r(e);
		}
	});
}
function rt(e, t) {
	return W ? e.setLocalDescription(t) : new Promise((n, r) => {
		try {
			e.setLocalDescription(t, n, r);
		} catch (e) {
			r(e);
		}
	});
}
function it(e, t) {
	return W ? e.setRemoteDescription(t) : new Promise((n, r) => {
		try {
			e.setRemoteDescription(t, n, r);
		} catch (e) {
			r(e);
		}
	});
}
//#endregion
//#region src/webtorrent/webtorrent-client/index.ts
var at = 5e4, ot = 15e3, st = 5, ct = 5e3;
function lt() {
	let e = "";
	for (let t = 0; t < 20; t++) e += "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789".charAt(Math.floor(Math.random() * 62));
	return e;
}
var ut = /* @__PURE__ */ new Set([
	"offer",
	"answer",
	"pranswer",
	"rollback"
]);
function dt(e) {
	if (typeof e != "object" || !e) return !1;
	let t = e;
	return typeof t.type == "string" && ut.has(t.type) && typeof t.sdp == "string";
}
var ft = class e {
	static #e = 120;
	static #t = 20;
	#n;
	#r;
	#i = new V();
	#a = /* @__PURE__ */ new Map();
	#o = /* @__PURE__ */ new Set();
	#s = new re();
	#c = null;
	#l = null;
	#u = 0;
	#d = null;
	#f = void 0;
	#p = null;
	#m = !1;
	#h() {
		return this.#s.signal.aborted;
	}
	#g() {
		if (this.#s.signal.aborted) throw Error("Client destroyed");
	}
	constructor(e) {
		this.#n = {
			infoHash: e.infoHash,
			peerId: e.peerId,
			rtcConfig: e.rtcConfig,
			channelConfig: e.channelConfig,
			offerTimeout: e.offerTimeout ?? (() => at),
			offersCount: e.offersCount ?? (() => st),
			iceGatheringTimeout: e.iceGatheringTimeout ?? (() => ct),
			connectionTimeout: e.connectionTimeout ?? (() => ot),
			claimPeer: e.claimPeer ?? (() => !0),
			shouldGenerateOffers: e.shouldGenerateOffers ?? (() => !0)
		}, this.#r = e.wsClient;
	}
	addEventListener(e, t) {
		this.#i.addEventListener(e, t);
	}
	removeEventListener(e, t) {
		this.#i.removeEventListener(e, t);
	}
	start() {
		this.#h() || this.#m || (this.#m = !0, this.#r.addEventListener("connected", this.#_), this.#r.addEventListener("disconnected", this.#v), this.#r.addEventListener("message", this.#y), this.#r.state === "connected" && this.#_());
	}
	destroy() {
		this.#h() || (this.#s.abort(), this.#x(), this.#w(), this.#j(), this.#M(), this.#m && (this.#r.removeEventListener("connected", this.#_), this.#r.removeEventListener("disconnected", this.#v), this.#r.removeEventListener("message", this.#y)), this.#i.clear());
	}
	#_ = () => {
		this.#b(e.#e), this.#S("started").catch((e) => {
			this.#h() || this.#i.dispatchEvent("error", new p("announce-failed", `Initial announce failed: ${e instanceof Error ? e.message : String(e)}`, e));
		});
	};
	#v = () => {
		this.#x(), this.#l = null;
	};
	#y = (t) => {
		if (this.#h()) return;
		let n;
		try {
			let e = typeof t == "string" ? t : new TextDecoder().decode(t);
			n = JSON.parse(e);
		} catch (e) {
			this.#i.dispatchEvent("error", new p("parse-error", `Failed to parse tracker message: ${e instanceof Error ? e.message : String(e)}`, e));
			return;
		}
		if (typeof n != "object" || !n || Array.isArray(n)) return;
		let r = n, i = r.info_hash;
		if (typeof i == "string" && i !== this.#n.infoHash) return;
		let a = r["warning message"];
		typeof a == "string" && this.#i.dispatchEvent("warning", new m("tracker-response", a));
		let o = r["failure reason"];
		if (typeof o == "string") {
			this.#i.dispatchEvent("error", new p("tracker-response", o));
			return;
		}
		let { interval: s } = r;
		if (typeof s == "number" && s > 0) {
			let t = Math.max(e.#t, s);
			this.#l !== t && this.#b(t);
		}
		let c = r["tracker id"];
		typeof c == "string" && (this.#p = c);
		let l = r.peer_id;
		if (typeof l == "string" && l === this.#n.peerId) return;
		let u = r.offer_id;
		typeof l == "string" && typeof u == "string" && (dt(r.offer) ? this.#E({
			sdp: r.offer,
			peerId: l,
			offerId: u
		}).catch((e) => {
			this.#h() || this.#i.dispatchEvent("error", new p("signaling-failed", `Failed to handle offer: ${e instanceof Error ? e.message : String(e)}`, e));
		}) : dt(r.answer) && this.#D({
			sdp: r.answer,
			peerId: l,
			offerId: u
		}).catch((e) => {
			this.#h() || this.#i.dispatchEvent("error", new p("signaling-failed", `Failed to handle answer: ${e instanceof Error ? e.message : String(e)}`, e));
		}));
	};
	#b(e) {
		this.#x(), this.#l = e;
		let t = ++this.#u, n = async () => {
			try {
				await this.#S();
			} catch (e) {
				if (this.#h()) return;
				this.#i.dispatchEvent("error", new p("announce-failed", `Announce failed: ${e instanceof Error ? e.message : String(e)}`, e));
			}
			!this.#h() && this.#l !== null && this.#u === t && (this.#c = setTimeout(n, this.#l * 1e3));
		};
		this.#c = setTimeout(n, e * 1e3);
	}
	#x() {
		this.#c !== null && (clearTimeout(this.#c), this.#c = null);
	}
	async #S(e) {
		if (this.#h() || this.#r.state !== "connected") return;
		if (e && (this.#f = e), this.#d) return this.#d;
		let t = (async () => {
			let e = this.#n.shouldGenerateOffers() ? this.#n.offersCount() : 0, t = await Promise.all(Array.from({ length: e }, () => this.#C()));
			if (this.#h()) {
				for (let e of t) e && this.#A(e.offer_id);
				return;
			}
			let n = [];
			for (let e of t) e && n.push(e);
			let r = this.#f;
			this.#f = void 0;
			let i = this.#T({
				numwant: n.length,
				offers: n,
				event: r
			});
			if (this.#r.state !== "connected") {
				for (let e of n) this.#A(e.offer_id);
				return;
			}
			try {
				this.#r.send(JSON.stringify(i));
			} catch (e) {
				for (let e of n) this.#A(e.offer_id);
				throw e;
			}
		})();
		this.#d = t;
		try {
			await t;
		} finally {
			this.#d === t && (this.#d = null);
		}
	}
	async #C() {
		if (this.#h()) return;
		let e;
		try {
			e = new U(this.#n.rtcConfig?.()), this.#o.add(e);
			let t = e.createDataChannel("webtorrent", this.#n.channelConfig), n = await tt(e);
			this.#g(), await rt(e, n), this.#g(), await this.#O(e), this.#g();
			let r = e.localDescription;
			if (!r?.sdp) {
				e.close();
				return;
			}
			let i = lt();
			return this.#a.set(i, {
				connection: e,
				channel: t,
				timeoutId: setTimeout(() => {
					this.#A(i);
				}, this.#n.offerTimeout())
			}), {
				offer: {
					type: r.type,
					sdp: r.sdp
				},
				offer_id: i
			};
		} catch (t) {
			e?.close(), this.#h() || this.#i.dispatchEvent("warning", new m("offer-failed", `Failed to create offer: ${t instanceof Error ? t.message : String(t)}`, t));
		} finally {
			e && this.#o.delete(e);
		}
	}
	#w() {
		if (this.#r.state !== "connected") return;
		let e = this.#T({
			numwant: 0,
			offers: [],
			event: "stopped"
		});
		try {
			this.#r.send(JSON.stringify(e));
		} catch {}
	}
	#T({ numwant: e, offers: t, event: n }) {
		let r = {
			action: "announce",
			info_hash: this.#n.infoHash,
			peer_id: this.#n.peerId,
			numwant: e,
			uploaded: 0,
			downloaded: 0,
			offers: t
		};
		return n && (r.event = n), this.#p && (r.trackerid = this.#p), r;
	}
	async #E({ sdp: e, peerId: t, offerId: n }) {
		if (this.#h() || !this.#n.claimPeer(t)) return;
		let r;
		try {
			r = new U(this.#n.rtcConfig?.()), this.#o.add(r), await it(r, new et(e)), this.#g();
			let i = await nt(r);
			this.#g(), await rt(r, i), this.#g(), await this.#O(r), this.#g();
			let a = r.localDescription;
			if (!a) throw Error("Failed to get local description after ICE gathering");
			let o = {
				action: "announce",
				info_hash: this.#n.infoHash,
				peer_id: this.#n.peerId,
				to_peer_id: t,
				offer_id: n,
				answer: {
					type: a.type,
					sdp: a.sdp
				}
			};
			this.#r.send(JSON.stringify(o));
			let s = await this.#k(r);
			this.#g(), this.#i.dispatchEvent("peerConnected", {
				peerId: t,
				connection: r,
				channel: s
			});
		} catch (e) {
			r?.close(), this.#i.dispatchEvent("peerConnectFailed", {
				peerId: t,
				error: new h("connection-failed", e instanceof Error ? e.message : String(e), e)
			});
		} finally {
			r && this.#o.delete(r);
		}
	}
	async #D({ sdp: e, peerId: t, offerId: n }) {
		if (this.#h()) return;
		let r = this.#a.get(n);
		if (r) {
			if (this.#a.delete(n), clearTimeout(r.timeoutId), !this.#n.claimPeer(t)) {
				r.connection.close();
				return;
			}
			this.#o.add(r.connection);
			try {
				await it(r.connection, new et(e)), this.#g();
				let n = await this.#k(r.connection, r.channel);
				this.#g(), this.#i.dispatchEvent("peerConnected", {
					peerId: t,
					connection: r.connection,
					channel: n
				});
			} catch (e) {
				r.connection.close(), this.#i.dispatchEvent("peerConnectFailed", {
					peerId: t,
					error: new h("connection-failed", e instanceof Error ? e.message : String(e), e)
				});
			} finally {
				this.#o.delete(r.connection);
			}
		}
	}
	#O(e) {
		return new Promise((t, n) => {
			if (e.iceGatheringState === "complete") {
				t();
				return;
			}
			if (e.signalingState === "closed") {
				n(/* @__PURE__ */ Error("RTCPeerConnection closed"));
				return;
			}
			let r, i = () => {
				clearTimeout(r), e.removeEventListener("icegatheringstatechange", a), e.removeEventListener("icecandidate", o), e.removeEventListener("signalingstatechange", s), this.#s.signal.removeEventListener("abort", c);
			}, a = () => {
				e.iceGatheringState === "complete" && (i(), t());
			}, o = (e) => {
				e.candidate === null && (i(), t());
			}, s = () => {
				e.signalingState === "closed" && (i(), n(/* @__PURE__ */ Error("RTCPeerConnection closed")));
			}, c = () => {
				i(), n(/* @__PURE__ */ Error("ICE gathering aborted due to teardown"));
			};
			if (this.#s.signal.aborted) {
				c();
				return;
			}
			r = setTimeout(() => {
				i(), t();
			}, this.#n.iceGatheringTimeout()), e.addEventListener("icegatheringstatechange", a), e.addEventListener("icecandidate", o), e.addEventListener("signalingstatechange", s), this.#s.signal.addEventListener("abort", c);
		});
	}
	#k(e, t) {
		let { promise: n, resolve: r, reject: i } = le(), a, o = t, s = () => L(e.iceConnectionState) ? (m(), i(/* @__PURE__ */ Error(`ICE connection ${e.iceConnectionState}`)), !0) : !1, c = () => {
			m(), o ? r(o) : i(/* @__PURE__ */ Error("Data channel missing on open"));
		}, l = () => {
			m(), i(/* @__PURE__ */ Error("Data channel error"));
		}, u = () => {
			m(), i(/* @__PURE__ */ Error("Data channel closed prematurely"));
		}, d = (e) => {
			o = e, e.readyState === "open" ? c() : e.readyState === "closed" || e.readyState === "closing" ? u() : (e.addEventListener("open", c), e.addEventListener("error", l), e.addEventListener("close", u), e.addEventListener("closing", u));
		}, f = (e) => {
			o || d(e.channel);
		}, p = () => {
			m(), i(/* @__PURE__ */ Error("Connection aborted due to teardown"));
		}, m = () => {
			clearTimeout(a), e.removeEventListener("iceconnectionstatechange", s), e.removeEventListener("datachannel", f), o && (o.removeEventListener("open", c), o.removeEventListener("error", l), o.removeEventListener("close", u), o.removeEventListener("closing", u)), this.#s.signal.removeEventListener("abort", p);
		};
		return this.#s.signal.aborted ? (p(), n) : s() ? n : (a = setTimeout(() => {
			m(), i(/* @__PURE__ */ Error("Data channel open timeout"));
		}, this.#n.connectionTimeout()), e.addEventListener("iceconnectionstatechange", s), this.#s.signal.addEventListener("abort", p), o ? d(o) : e.addEventListener("datachannel", f), n);
	}
	#A(e, t) {
		let n = t ?? this.#a.get(e);
		n && (clearTimeout(n.timeoutId), n.connection.close(), this.#a.delete(e));
	}
	#j() {
		for (let [e, t] of this.#a) this.#A(e, t);
	}
	#M() {
		for (let e of this.#o) e.close();
		this.#o.clear();
	}
}, pt = 50, mt = 1.5, ht = class {
	#e;
	#t = new V();
	#n = /* @__PURE__ */ new Set();
	#r = /* @__PURE__ */ new Map();
	#i = /* @__PURE__ */ new Set();
	#a = !1;
	#o = !1;
	#s = (e) => {
		if (this.#a || this.#n.has(e) || this.#r.has(e)) return !1;
		let t = Math.floor(this.#e.maxPeers() * Math.max(1, this.#e.maxPeersMultiplier()));
		return this.#n.size + this.#r.size >= t ? !1 : (this.#n.add(e), !0);
	};
	constructor(e) {
		this.#e = {
			...e,
			maxPeers: e.maxPeers ?? (() => pt),
			maxPeersMultiplier: e.maxPeersMultiplier ?? (() => mt)
		};
	}
	addEventListener(e, t) {
		this.#t.addEventListener(e, t);
	}
	removeEventListener(e, t) {
		this.#t.removeEventListener(e, t);
	}
	start() {
		if (!(this.#a || this.#o)) {
			this.#o = !0;
			try {
				for (let e of this.#e.trackerUrls) {
					let { client: t, release: n } = this.#e.socketPool.acquire(e), r = !1;
					try {
						let i = new ft({
							infoHash: this.#e.infoHash,
							peerId: this.#e.peerId,
							wsClient: t,
							rtcConfig: this.#e.rtcConfig,
							channelConfig: this.#e.channelConfig,
							claimPeer: this.#s,
							offersCount: this.#e.offersCount,
							offerTimeout: this.#e.offerTimeout,
							iceGatheringTimeout: this.#e.iceGatheringTimeout,
							connectionTimeout: this.#e.connectionTimeout,
							shouldGenerateOffers: () => this.#n.size + this.#r.size < this.#e.maxPeers()
						}), a = (t) => {
							this.#n.delete(t.peerId), this.#l(t.peerId, t.connection, t.channel, e);
						}, o = (t) => {
							this.#n.has(t.peerId) && (this.#n.delete(t.peerId), this.#t.dispatchEvent("peerConnectFailed", {
								peerId: t.peerId,
								trackerUrl: e,
								error: t.error
							}));
						}, s = (t) => {
							this.#t.dispatchEvent("warning", {
								trackerUrl: e,
								warning: t
							});
						}, c = (t) => {
							this.#t.dispatchEvent("error", {
								trackerUrl: e,
								error: t
							});
						};
						i.addEventListener("peerConnected", a), i.addEventListener("peerConnectFailed", o), i.addEventListener("warning", s), i.addEventListener("error", c), this.#i.add({
							client: i,
							releaseSocket: n,
							cleanupListeners: () => {
								i.removeEventListener("peerConnected", a), i.removeEventListener("peerConnectFailed", o), i.removeEventListener("warning", s), i.removeEventListener("error", c);
							}
						}), r = !0, i.start();
					} catch (e) {
						throw r || n(), e;
					}
				}
			} catch (e) {
				throw this.destroy(), e;
			}
		}
	}
	destroy() {
		if (this.#a) return;
		this.#a = !0;
		for (let { client: e, releaseSocket: t, cleanupListeners: n } of this.#i) n(), e.destroy(), t();
		this.#i.clear(), this.#n.clear();
		let e = [...this.#r.entries()];
		this.#r.clear();
		for (let [t, n] of e) {
			n.cleanup();
			try {
				n.channel.close();
			} catch {}
			try {
				n.connection.close();
			} catch {}
			this.#t.dispatchEvent("peerDisconnected", {
				peerId: t,
				trackerUrl: n.trackerUrl,
				disconnectReason: "Manager destroyed"
			});
		}
		this.#t.clear();
	}
	#c(e, t) {
		if (this.#a) return;
		let n = this.#r.get(e);
		if (n) {
			this.#r.delete(e), n.cleanup();
			try {
				n.channel.close();
			} catch {}
			try {
				n.connection.close();
			} catch {}
			this.#t.dispatchEvent("peerDisconnected", {
				peerId: e,
				trackerUrl: n.trackerUrl,
				...t
			});
		}
	}
	#l(e, t, n, r) {
		if (L(t.iceConnectionState)) {
			try {
				t.close();
			} catch {}
			this.#t.dispatchEvent("peerConnectFailed", {
				peerId: e,
				trackerUrl: r,
				error: new h("connection-failed", "Connection failed during promotion")
			});
			return;
		}
		let i = (t) => this.#c(e, t), a = () => {
			L(t.iceConnectionState) && i({ error: new d("connection-lost", `ICE connection state became ${t.iceConnectionState}`) });
		}, o = () => i({ disconnectReason: "Data channel closed" }), s = () => i({ disconnectReason: "Data channel closing" }), c = (e) => {
			let t = Je(e, "Data channel error");
			i({ error: new d("transport-error", `Data channel error: ${t}`) });
		}, l = (t) => this.#c(e, t ? { error: t } : { disconnectReason: "Closed by consumer" });
		this.#r.set(e, {
			connection: t,
			channel: n,
			trackerUrl: r,
			cleanup: () => {
				l = null, t.removeEventListener("iceconnectionstatechange", a), n.removeEventListener("close", o), n.removeEventListener("closing", s), n.removeEventListener("error", c);
			}
		}), t.addEventListener("iceconnectionstatechange", a), n.addEventListener("close", o), n.addEventListener("closing", s), n.addEventListener("error", c), this.#t.dispatchEvent("peerConnected", {
			peerId: e,
			connection: t,
			channel: n,
			trackerUrl: r,
			close: (e) => l?.(e)
		});
	}
};
//#endregion
//#region src/utils/stream.ts
function gt(e, t) {
	for (let n of e.values()) {
		let e = n.segments.get(t);
		if (e) return e;
	}
}
function _t(e, t) {
	for (let n of e.segments.values()) if (n.externalId === t) return n;
}
function vt(e) {
	let { segments: t } = e, n = 0, { size: r } = t;
	if (r === 0) return 0;
	for (let e of t.values()) {
		let t = e.endTime - e.startTime;
		n += t;
	}
	return n / r;
}
function yt(e, t) {
	let { highDemandTimeWindow: n, httpDownloadTimeWindow: r, p2pDownloadTimeWindow: i } = e, a = {
		highDemandTimeWindow: n,
		httpDownloadTimeWindow: r,
		p2pDownloadTimeWindow: i
	};
	return t <= 5 ? (a.httpDownloadTimeWindow = 0, a.p2pDownloadTimeWindow = 0) : t <= 10 && (a.p2pDownloadTimeWindow = a.httpDownloadTimeWindow), a;
}
function G(e, t, n, r, i) {
	let { highDemandTimeWindow: a, httpDownloadTimeWindow: o, p2pDownloadTimeWindow: s } = yt(n, i);
	return {
		isHighDemand: K(e, t, a),
		isHttpDownloadable: K(e, t, o),
		isP2PDownloadable: K(e, t, s) && r.isSegmentLoadingOrLoadedBySomeone(e)
	};
}
function K(e, t, n) {
	let { startTime: r, endTime: i } = e, { position: a, rate: o } = t;
	return !(a + n * o < r || a > i);
}
//#endregion
//#region src/p2p/loader.ts
var bt = 1e3, xt = class {
	#e;
	#t = /* @__PURE__ */ new Map();
	#n = !1;
	#r = (0, v.default)("p2pml-core:webtorrent-manager");
	#i = (0, v.default)("p2pml-core:churn-cleanup");
	#a;
	#o;
	#s;
	#c;
	#l;
	#u;
	#d;
	#f;
	#p;
	#m;
	#h;
	#g;
	#_;
	#v;
	#y;
	constructor(e, t, n, r, i, a, o, s) {
		this.#a = e, this.#o = t, this.#s = n, this.#c = r, this.#l = i, this.#u = a, this.#d = s, this.#p = a.getEventDispatcher("onPeerConnect"), this.#m = a.getEventDispatcher("onPeerConnectError"), this.#h = a.getEventDispatcher("onPeerClose"), this.#g = a.getEventDispatcher("onPeerError"), this.#_ = a.getEventDispatcher("onPeerWarning"), this.#v = a.getEventDispatcher("onTrackerWarning"), this.#y = a.getEventDispatcher("onTrackerError"), this.#e = new ht({
			infoHash: this.#a.infoHash,
			peerId: o,
			trackerUrls: this.#c.announceTrackers,
			rtcConfig: () => this.#c.rtcConfig,
			socketPool: this.#l,
			maxPeers: () => this.#c.p2pMaxPeers,
			maxPeersMultiplier: () => this.#c.p2pChurnMaxPeersMultiplier,
			offersCount: () => this.#c.webRtcOffersCount,
			offerTimeout: () => this.#c.webRtcOfferTimeoutMs,
			iceGatheringTimeout: () => this.#c.webRtcIceGatheringTimeoutMs,
			connectionTimeout: () => this.#c.webRtcConnectionTimeoutMs
		}), this.#e.addEventListener("peerConnected", this.#S), this.#e.addEventListener("peerDisconnected", this.#C), this.#e.addEventListener("peerConnectFailed", (e) => {
			this.#r(`Peer connection failed (${e.peerId}) from tracker ${e.trackerUrl}:`, e.error), this.#m({
				peerId: e.peerId,
				infoHash: this.#a.infoHash,
				streamType: this.#a.type,
				trackerUrl: e.trackerUrl,
				error: e.error
			});
		}), this.#e.addEventListener("warning", (e) => {
			this.#r(`Tracker warning (${e.trackerUrl}):`, e.warning), this.#v({
				trackerUrl: e.trackerUrl,
				infoHash: this.#a.infoHash,
				streamType: this.#a.type,
				warning: e.warning
			});
		}), this.#e.addEventListener("error", (e) => {
			this.#r(`Tracker error (${e.trackerUrl}):`, e.error), this.#y({
				trackerUrl: e.trackerUrl,
				infoHash: this.#a.infoHash,
				streamType: this.#a.type,
				error: e.error
			});
		}), this.#u.addEventListener(`onStorageUpdated-${this.#a.streamSwarmId}`, this.broadcastAnnouncement), this.#e.start(), this.#f = setTimeout(this.#b, Math.max(bt, this.#c.p2pChurnCleanupIntervalMs));
	}
	#b = () => {
		this.#f = setTimeout(this.#b, Math.max(bt, this.#c.p2pChurnCleanupIntervalMs));
		let e = this.#t.size - this.#c.p2pMaxPeers;
		if (e <= 0) return;
		let t = [], n = performance.now();
		for (let e of this.#t.values()) e.downloadingSegment || e.isUploadingSegment || n - e.connectedAt < this.#c.p2pChurnGracePeriodMs || t.push(e);
		if (t.length === 0) return;
		t.sort((e, t) => e.getDownloadBandwidth() - t.getDownloadBandwidth() || e.connectedAt - t.connectedAt);
		let r = t.slice(0, e);
		this.#i(`Background churn cleanup: dropping ${r.length} excess peers (total: ${this.#t.size}, target: ${this.#c.p2pMaxPeers}, eligible: ${t.length})`);
		for (let e of r) this.#i(`dropping excess peer ${e.id} with bandwidth ${e.getDownloadBandwidth()}`), e.destroy();
	};
	downloadSegment(e) {
		let t = [];
		for (let n of this.#t.values()) !n.downloadingSegment && n.getSegmentStatus(e) === "loaded" && t.push(n);
		if (t.length === 0) return;
		let n = St(t), r = this.#o.getOrCreateRequest(e);
		n.downloadSegment(r);
	}
	isSegmentLoadingOrLoadedBySomeone(e) {
		for (let t of this.#t.values()) if (t.getSegmentStatus(e)) return !0;
		return !1;
	}
	isSegmentLoadedBySomeone(e) {
		for (let t of this.#t.values()) if (t.getSegmentStatus(e) === "loaded") return !0;
		return !1;
	}
	get connectedPeerCount() {
		return this.#t.size;
	}
	*peers() {
		for (let e of this.#t.values()) yield e;
	}
	#x() {
		let e = this.#s.getStoredSegmentIds(this.#a.swarmId, this.#a.streamSwarmId), t = [];
		for (let e of this.#o.httpRequests()) {
			let n = this.#a.segments.get(e.segment.runtimeId);
			n && t.push(n.externalId);
		}
		return {
			loaded: e,
			httpLoading: t
		};
	}
	#S = (e) => {
		if (this.#r(`peerConnected: peerId=${e.peerId}`), this.#t.has(e.peerId)) {
			e.close();
			return;
		}
		let t = new $e(e.peerId, e.channel, e.close, {
			onSegmentRequested: (e, t, n, r) => {
				this.#T(e, t, n, r).catch((n) => {
					this.#r(`Error in onSegmentRequested ${t} for peer ${e.id}:`, n);
				});
			},
			onSegmentsAnnouncement: this.#d,
			onWarning: (n) => {
				this.#_({
					peerId: t.id,
					infoHash: this.#a.infoHash,
					streamType: this.#a.type,
					trackerUrl: e.trackerUrl,
					warning: n
				});
			}
		}, {
			p2pNotReceivingBytesTimeoutMs: this.#c.p2pNotReceivingBytesTimeoutMs,
			webRtcMaxMessageSize: this.#c.webRtcMaxMessageSize,
			p2pErrorRetries: this.#c.p2pErrorRetries,
			validateP2PSegment: this.#c.validateP2PSegment,
			streamType: this.#a.type,
			infoHash: this.#a.infoHash
		}, this.#u);
		if (this.#t.set(e.peerId, t), this.#p({
			peerId: e.peerId,
			infoHash: this.#a.infoHash,
			streamType: this.#a.type,
			trackerUrl: e.trackerUrl
		}), this.#c.isP2PUploadDisabled) return;
		let { httpLoading: n, loaded: r } = this.#x();
		t.sendSegmentsAnnouncementCommand(r, n);
	};
	#C = (e) => {
		this.#r("peerDisconnected: peerId=%s error=%s reason=%s", e.peerId, e.error?.message, e.disconnectReason);
		let t = this.#t.get(e.peerId);
		t && (this.#t.delete(e.peerId), t.destroy(!0), e.error && this.#g({
			peerId: e.peerId,
			infoHash: this.#a.infoHash,
			streamType: this.#a.type,
			trackerUrl: e.trackerUrl,
			error: e.error
		}), this.#h({
			peerId: t.id,
			infoHash: this.#a.infoHash,
			streamType: this.#a.type,
			trackerUrl: e.trackerUrl
		}));
	};
	broadcastAnnouncement = (e = !1) => {
		if (e) {
			this.#w(e);
			return;
		}
		this.#n || this.#c.isP2PUploadDisabled || this.#w();
	};
	#w = (e = !1) => {
		this.#n = !0, ue(() => {
			let { loaded: t = [], httpLoading: n = [] } = e ? {} : this.#x();
			for (let e of this.#t.values()) e.sendSegmentsAnnouncementCommand(t, n);
			this.#n = !1;
		});
	};
	#T = async (e, t, n, r) => {
		let i = _t(this.#a, t);
		if (!i) return;
		if (this.#c.isP2PUploadDisabled) {
			e.sendSegmentAbsentCommand(t, n);
			return;
		}
		let a;
		try {
			a = await this.#s.getSegmentData(this.#a.swarmId, this.#a.streamSwarmId, i.externalId);
		} catch (n) {
			this.#r(`Storage error for segment ${t} requested by peer ${e.id}:`, n);
		}
		if (this.#t.has(e.id)) {
			if (!a) {
				e.sendSegmentAbsentCommand(t, n);
				return;
			}
			await e.uploadSegmentData(i, n, r === void 0 ? a : new Uint8Array(a).subarray(r));
		}
	};
	destroy() {
		clearTimeout(this.#f), this.#f = void 0, this.#u.removeEventListener(`onStorageUpdated-${this.#a.streamSwarmId}`, this.broadcastAnnouncement), this.#e.destroy();
		for (let e of this.#t.values()) e.destroy();
		this.#t.clear();
	}
};
function St(e) {
	if (e.length === 1) return e[0];
	let t = 0;
	for (let n of e) {
		let e = n.getDownloadBandwidth();
		e > t && (t = e);
	}
	if (t > 0) {
		let n = Math.max(1, t * .1), r = 0, i = 0;
		for (let t of e) t.getDownloadBandwidth() <= n ? r++ : i += t.getDownloadBandwidth();
		let a = n;
		return r > 0 && i > 0 && r * n > i && (a = i / r), pe(e, (e) => Math.max(e.getDownloadBandwidth(), a));
	}
	return fe(e);
}
//#endregion
//#region src/utils/logger.ts
function q(e) {
	return `${e.type}-${e.identityHash}`;
}
function J(e) {
	let { externalId: t } = e;
	return `(${q(e.stream)} | ${t})`;
}
//#endregion
//#region src/p2p/loaders-container.ts
var Ct = class {
	#e = /* @__PURE__ */ new Map();
	#t;
	#n = (0, v.default)("p2pml-core:p2p-loaders-container");
	#r;
	#i;
	#a;
	#o;
	#s;
	#c;
	#l;
	constructor(e, t, n, r, i, a, o, s) {
		this.#r = t, this.#i = n, this.#a = r, this.#o = i, this.#s = a, this.#c = o, this.#l = s, this.#t = this.#d(e), this.#n(`set current p2p loader: ${q(e)}`);
	}
	#u(e) {
		if (this.#e.has(e.runtimeId)) throw Error("Loader for this stream already exists");
		let t = new xt(e, this.#r, this.#i, this.#a, this.#o, this.#s, this.#c, () => {
			this.#t.loader === t && this.#l();
		}), n = q(e);
		return this.#n(`created new loader: ${n}`), {
			loader: t,
			stream: e,
			loggerInfo: n
		};
	}
	#d(e) {
		let t = this.#e.get(e.runtimeId);
		if (t) return clearTimeout(t.destroyTimeoutId), t.destroyTimeoutId = void 0, t;
		{
			let t = this.#u(e);
			return this.#e.set(e.runtimeId, t), t;
		}
	}
	changeCurrentLoader(e) {
		let t = this.#t.stream;
		this.#i.getStoredSegmentIds(t.swarmId, t.streamSwarmId).length ? this.#f(this.#t) : this.#p(this.#t), this.#t = this.#d(e), this.#n(`change current p2p loader: ${q(e)}`);
	}
	#f(e) {
		e.destroyTimeoutId = window.setTimeout(() => this.#p(e), this.#a.p2pInactiveLoaderDestroyTimeoutMs);
	}
	#p(e) {
		e.loader.destroy(), this.#e.delete(e.stream.runtimeId), this.#n("destroy p2p loader: ", e.loggerInfo);
	}
	get currentLoader() {
		return this.#t.loader;
	}
	destroy() {
		for (let { loader: e, destroyTimeoutId: t } of this.#e.values()) e.destroy(), clearTimeout(t);
		this.#e.clear();
	}
};
//#endregion
//#region src/requests/request.ts
function Y(e) {
	return {
		runtimeId: e.runtimeId,
		externalId: e.externalId,
		url: e.url,
		byteRange: e.byteRange && { ...e.byteRange },
		startTime: e.startTime,
		endTime: e.endTime
	};
}
var wt = class {
	segment;
	requestProcessQueueCallback;
	bandwidthCalculators;
	playback;
	playbackConfig;
	infoHash;
	currentAttempt;
	_failedAttempts = new Tt();
	finalData;
	bytes = [];
	_loadedBytes = 0;
	_totalBytes;
	_status = "not-started";
	progress;
	notReceivingBytesTimeout;
	_onAbortCallback;
	notReceivingBytesTimeoutMs;
	_logger;
	_isHandledByProcessQueue = !1;
	onSegmentError;
	onSegmentAbort;
	onSegmentStart;
	onSegmentLoaded;
	constructor(e, t, n, r, i, a, o) {
		this.segment = e, this.requestProcessQueueCallback = t, this.bandwidthCalculators = n, this.playback = r, this.playbackConfig = i, this.infoHash = o, this.onSegmentError = a.getEventDispatcher("onSegmentError"), this.onSegmentAbort = a.getEventDispatcher("onSegmentAbort"), this.onSegmentStart = a.getEventDispatcher("onSegmentStart"), this.onSegmentLoaded = a.getEventDispatcher("onSegmentLoaded");
		let { byteRange: s } = this.segment;
		if (s) {
			let { end: e, start: t } = s;
			this._totalBytes = e - t + 1;
		}
		this.notReceivingBytesTimeout = new Et(this.abortOnTimeout);
		let { type: c } = this.segment.stream;
		this._logger = (0, v.default)(`p2pml-core:request-${c}`);
	}
	clearLoadedBytes() {
		this._loadedBytes = 0, this.bytes = [], this._totalBytes = void 0, this.finalData = void 0;
	}
	get status() {
		return this._status;
	}
	setStatus(e) {
		this._status = e, this._isHandledByProcessQueue = !1;
	}
	get downloadSource() {
		return this.currentAttempt?.downloadSource;
	}
	get loadedBytes() {
		return this._loadedBytes;
	}
	get totalBytes() {
		return this._totalBytes;
	}
	get data() {
		return this.finalData ??= de(this.bytes).buffer, this.finalData;
	}
	get failedAttempts() {
		return this._failedAttempts;
	}
	get isHandledByProcessQueue() {
		return this._isHandledByProcessQueue;
	}
	markHandledByProcessQueue() {
		this._isHandledByProcessQueue = !0;
	}
	setTotalBytes(e) {
		if (this._totalBytes !== void 0) throw Error("Request total bytes value is already set");
		this._totalBytes = e;
	}
	tryCompleteByLoadedBytes(e, t, n, r) {
		if (!this._totalBytes) return !1;
		if (this._loadedBytes > this._totalBytes) return this.logger(`${e.downloadSource} ${this.segment.externalId} loaded bytes overflow: ${this._loadedBytes} > ${this._totalBytes}, clearing`), this.clearLoadedBytes(), !1;
		if (this._loadedBytes !== this._totalBytes) return !1;
		let i = this.start(e, t);
		return this.notReceivingBytesTimeout.clear(), n ? this.validateAndComplete(e.downloadSource, i, n, r) : i.completeOnSuccess(), !0;
	}
	async validateData(e) {
		if (!e) return !0;
		try {
			return await e(this.segment.url, this.segment.byteRange, this.data);
		} catch (e) {
			return this.logger(`validation threw an error: ${String(e)}`), !1;
		}
	}
	async validateAndComplete(e, t, n, r) {
		let i = await this.validateData(n);
		if (this._status === "loading") {
			if (!i) {
				this.logger(`${e} ${this.segment.externalId} validation failed for already-loaded bytes, clearing`), this.clearLoadedBytes(), t.failWithError(new g(r));
				return;
			}
			this.logger(`${e} ${this.segment.externalId} validation passed for already-loaded bytes`), t.completeOnSuccess();
		}
	}
	start(e, t) {
		if (this._status === "succeed") throw Error(`Request ${this.segment.externalId} has been already succeed.`);
		if (this._status === "loading") throw Error(`Request ${this.segment.externalId} has been already started.`);
		this.setStatus("loading"), this.currentAttempt = { ...e }, this.progress = {
			startFromByte: this._loadedBytes,
			loadedBytes: 0,
			startTimestamp: performance.now()
		}, this.manageBandwidthCalculatorsState("start");
		let { notReceivingBytesTimeoutMs: n } = t;
		return this._onAbortCallback = t.onAbort, this.notReceivingBytesTimeoutMs = n, n !== void 0 && this.notReceivingBytesTimeout.start(n), this.logger(`${e.downloadSource} ${this.segment.externalId} started`), this.onSegmentStart({
			segment: Y(this.segment),
			downloadSource: e.downloadSource,
			peerId: e.downloadSource === "p2p" ? e.peerId : void 0,
			infoHash: this.infoHash,
			streamType: this.segment.stream.type
		}), {
			firstBytesReceived: this.firstBytesReceived,
			addLoadedChunk: this.addLoadedChunk,
			completeOnSuccess: this.completeOnSuccess,
			failWithError: this.failWithError
		};
	}
	cancel() {
		this.throwErrorIfNotLoadingStatus(), this.setStatus("aborted"), this.logger(`${this.currentAttempt?.downloadSource} ${this.segment.externalId} aborted`), this._onAbortCallback?.(new g("abort")), this.onSegmentAbort({
			segment: Y(this.segment),
			downloadSource: this.currentAttempt?.downloadSource,
			peerId: this.currentAttempt?.downloadSource === "p2p" ? this.currentAttempt.peerId : void 0,
			infoHash: this.infoHash,
			streamType: this.segment.stream.type
		}), this._onAbortCallback = void 0, this.manageBandwidthCalculatorsState("stop"), this.notReceivingBytesTimeout.clear();
	}
	abortOnTimeout = () => {
		if (this.throwErrorIfNotLoadingStatus(), !this.currentAttempt || !this.progress || this.notReceivingBytesTimeoutMs === void 0) return;
		let e = performance.now() - (this.progress.lastLoadedChunkTimestamp ?? this.progress.startTimestamp);
		if (e < this.notReceivingBytesTimeoutMs) {
			this.notReceivingBytesTimeout.restart(this.notReceivingBytesTimeoutMs - e);
			return;
		}
		let t = new g("bytes-receiving-timeout");
		this._onAbortCallback?.(t), this.handleFailure(t);
	};
	failWithError = (e) => {
		this.throwErrorIfNotLoadingStatus(), this.currentAttempt && this.handleFailure(e);
	};
	handleFailure = (e) => {
		this.currentAttempt && (this.setStatus("failed"), this.logger(`${this.downloadSource} ${this.segment.externalId} failed ${e.type}`), this._failedAttempts.add({
			...this.currentAttempt,
			error: e
		}), this.onSegmentError({
			segment: Y(this.segment),
			error: e,
			downloadSource: this.currentAttempt.downloadSource,
			peerId: this.currentAttempt.downloadSource === "p2p" ? this.currentAttempt.peerId : void 0,
			infoHash: this.infoHash,
			streamType: this.segment.stream.type
		}), this.notReceivingBytesTimeout.clear(), this.manageBandwidthCalculatorsState("stop"), this.requestProcessQueueCallback());
	};
	completeOnSuccess = () => {
		this.throwErrorIfNotLoadingStatus(), this.currentAttempt && (this.manageBandwidthCalculatorsState("stop"), this.notReceivingBytesTimeout.clear(), this.setStatus("succeed"), this._totalBytes = this._loadedBytes, this.onSegmentLoaded({
			segment: Y(this.segment),
			bytesLength: this.data.byteLength,
			downloadSource: this.currentAttempt.downloadSource,
			peerId: this.currentAttempt.downloadSource === "p2p" ? this.currentAttempt.peerId : void 0,
			infoHash: this.infoHash,
			streamType: this.segment.stream.type
		}), this.logger(`${this.currentAttempt.downloadSource} ${this.segment.externalId} succeed`), this.requestProcessQueueCallback());
	};
	addLoadedChunk = (e) => {
		if (this.throwErrorIfNotLoadingStatus(), !this.currentAttempt || !this.progress) return;
		let { byteLength: t } = e, { all: n, http: r } = this.bandwidthCalculators;
		n.addBytes(t), this.currentAttempt.downloadSource === "http" && r.addBytes(t), this.bytes.push(e), this.progress.lastLoadedChunkTimestamp = performance.now(), this.progress.loadedBytes += t, this._loadedBytes += t;
	};
	firstBytesReceived = () => {
		this.throwErrorIfNotLoadingStatus();
	};
	throwErrorIfNotLoadingStatus() {
		if (this._status !== "loading") throw Error(`Request has been already ${this.status}.`);
	}
	logger(e) {
		this._logger.color = this.currentAttempt?.downloadSource === "http" ? "green" : "red", this._logger(e), this._logger.color = "";
	}
	manageBandwidthCalculatorsState(e) {
		let { all: t, http: n } = this.bandwidthCalculators, r = e === "start" ? "startLoading" : "stopLoading";
		this.currentAttempt?.downloadSource === "http" && n[r](), t[r]();
	}
}, Tt = class {
	attempts = [];
	add(e) {
		this.attempts.push(e);
	}
	get httpAttemptsCount() {
		return this.attempts.reduce((e, t) => t.downloadSource === "http" ? e + 1 : e, 0);
	}
	get p2pAttemptsCount() {
		return this.attempts.reduce((e, t) => t.downloadSource === "p2p" ? e + 1 : e, 0);
	}
	get lastAttempt() {
		return this.attempts[this.attempts.length - 1];
	}
	clear() {
		this.attempts = [];
	}
}, Et = class {
	action;
	timeoutId;
	ms;
	constructor(e) {
		this.action = e;
	}
	start(e) {
		if (this.timeoutId) throw Error("Timeout is already started.");
		this.ms = e, this.timeoutId = window.setTimeout(this.action, this.ms);
	}
	restart(e) {
		this.clear(), e !== void 0 && (this.ms = e), this.ms !== void 0 && (this.timeoutId = window.setTimeout(this.action, this.ms));
	}
	clear() {
		clearTimeout(this.timeoutId), this.timeoutId = void 0;
	}
}, Dt = class {
	requestProcessQueueCallback;
	bandwidthCalculators;
	playback;
	config;
	eventTarget;
	requests = /* @__PURE__ */ new Map();
	constructor(e, t, n, r, i) {
		this.requestProcessQueueCallback = e, this.bandwidthCalculators = t, this.playback = n, this.config = r, this.eventTarget = i;
	}
	get executingHttpCount() {
		let e = 0;
		for (let t of this.httpRequests()) t.status === "loading" && e++;
		return e;
	}
	get executingP2PCount() {
		let e = 0;
		for (let t of this.p2pRequests()) t.status === "loading" && e++;
		return e;
	}
	get(e) {
		return this.requests.get(e);
	}
	getOrCreateRequest(e) {
		let t = this.requests.get(e);
		return t || (t = new wt(e, this.requestProcessQueueCallback, this.bandwidthCalculators, this.playback, this.config, this.eventTarget, e.stream.infoHash), this.requests.set(e, t)), t;
	}
	remove(e) {
		this.requests.delete(e.segment);
	}
	items() {
		return this.requests.values();
	}
	*httpRequests() {
		for (let e of this.requests.values()) e.downloadSource === "http" && (yield e);
	}
	*p2pRequests() {
		for (let e of this.requests.values()) e.downloadSource === "p2p" && (yield e);
	}
	destroy() {
		for (let e of this.requests.values()) e.status === "loading" && e.cancel();
		this.requests.clear();
	}
}, Ot = class {
	segment;
	engineCallbacks;
	_status = "pending";
	_shouldBeStartedImmediately = !1;
	constructor(e, t) {
		this.segment = e, this.engineCallbacks = t;
	}
	get status() {
		return this._status;
	}
	get shouldBeStartedImmediately() {
		return this._shouldBeStartedImmediately;
	}
	resolve(e, t) {
		this._status === "pending" && (this._status = "succeed", this.engineCallbacks.onSuccess({
			data: e,
			bandwidth: t
		}));
	}
	reject() {
		this._status === "pending" && (this._status = "failed", this.engineCallbacks.onError(new _("failed")));
	}
	abort() {
		this._status === "pending" && (this._status = "aborted", this.engineCallbacks.onError(new _("aborted")));
	}
	markAsShouldBeStartedImmediately() {
		this._shouldBeStartedImmediately = !0;
	}
};
//#endregion
//#region src/utils/queue.ts
function* kt(e, t, n, r, i) {
	let { runtimeId: a, stream: o } = e, s = o.segments.get(a);
	if (!s) return;
	let c = o.segments.values(), l;
	do {
		let e = c.next();
		if (e.done) return;
		l = e.value;
	} while (l !== s);
	let u = G(l, t, n, r, i);
	if (X(u)) {
		let e = c.next();
		if (e.done) return;
		let a = e.value, o = G(a, t, n, r, i);
		if (X(o)) return;
		u.isHighDemand = !0, yield {
			segment: l,
			statuses: u
		}, yield {
			segment: a,
			statuses: o
		};
	} else yield {
		segment: l,
		statuses: u
	};
	for (let e of c) {
		let a = G(e, t, n, r, i);
		if (X(a)) break;
		yield {
			segment: e,
			statuses: a
		};
	}
}
function X(e) {
	let { isHighDemand: t, isHttpDownloadable: n, isP2PDownloadable: r } = e;
	return !t && !n && !r;
}
//#endregion
//#region src/hybrid-loader.ts
var At = 6e4, jt = 1e3, Mt = class {
	lastRequestedSegment;
	streamDetails;
	config;
	bandwidthCalculators;
	segmentStorage;
	webTorrentSocketPool;
	eventTarget;
	peerId;
	requests;
	engineRequest;
	p2pLoaders;
	playback;
	segmentAvgDuration;
	logger;
	levelChangedTimestamp;
	lastQueueProcessingTimeStamp;
	randomHttpDownloadTimeout;
	initialHttpDelayTimeoutId;
	isProcessQueueMicrotaskCreated = !1;
	createdAt = performance.now();
	constructor(e, t, n, r, i, a, o, s) {
		this.lastRequestedSegment = e, this.streamDetails = t, this.config = n, this.bandwidthCalculators = r, this.segmentStorage = i, this.webTorrentSocketPool = a, this.eventTarget = o, this.peerId = s;
		let c = this.lastRequestedSegment.stream;
		this.playback = {
			position: this.lastRequestedSegment.startTime,
			rate: 1
		}, this.segmentAvgDuration = vt(c), this.requests = new Dt(this.requestProcessQueueMicrotask, this.bandwidthCalculators, this.playback, this.config, this.eventTarget), this.p2pLoaders = new Ct(this.lastRequestedSegment.stream, this.requests, this.segmentStorage, this.config, this.webTorrentSocketPool, this.eventTarget, this.peerId, this.requestProcessQueueMicrotask), this.logger = (0, v.default)(`p2pml-core:hybrid-loader-${c.type}`), this.logger.color = "coral", this.setIntervalLoading();
	}
	setIntervalLoading() {
		let e = this.p2pLoaders.currentLoader.connectedPeerCount, t = Math.random() * jt * e + jt;
		this.randomHttpDownloadTimeout = window.setTimeout(() => {
			this.loadRandomThroughHttp(), this.setIntervalLoading();
		}, t);
	}
	async loadSegment(e, t) {
		this.logger(`requests: ${J(e)}`);
		let { stream: n } = e;
		n !== this.lastRequestedSegment.stream && (this.logger(`stream changed to ${q(n)}`), this.p2pLoaders.changeCurrentLoader(n)), this.lastRequestedSegment = e, this.segmentStorage.onSegmentRequested(n.swarmId, n.streamSwarmId, e.externalId, e.startTime, e.endTime, n.type, this.streamDetails.isLive);
		let r = new Ot(e, t);
		try {
			if (this.segmentStorage.hasSegment(n.swarmId, n.streamSwarmId, e.externalId)) {
				let t = await this.segmentStorage.getSegmentData(n.swarmId, n.streamSwarmId, e.externalId);
				if (t) {
					let { queueDownloadRatio: e } = this.generateQueue();
					r.resolve(t, this.getBandwidth(e));
					return;
				}
			}
			this.engineRequest?.abort(), this.engineRequest = r;
			let t = this.requests.get(e);
			t?.status === "failed" && t.failedAttempts.clear();
		} catch (t) {
			this.logger(`request failed for ${J(e)} in ${q(n)}`, t), r.reject();
		} finally {
			this.requestProcessQueueMicrotask();
		}
	}
	requestProcessQueueMicrotask = (e = !0) => {
		let t = performance.now();
		!e && this.lastQueueProcessingTimeStamp !== void 0 && t - this.lastQueueProcessingTimeStamp <= 1e3 || this.isProcessQueueMicrotaskCreated || (this.isProcessQueueMicrotaskCreated = !0, ue(() => {
			try {
				this.processQueue(), this.lastQueueProcessingTimeStamp = t;
			} finally {
				this.isProcessQueueMicrotaskCreated = !1;
			}
		}));
	};
	processRequests(e, t) {
		let { stream: n } = this.lastRequestedSegment, { httpErrorRetries: r } = this.config, i = performance.now();
		for (let a of this.requests.items()) {
			let { downloadSource: o, status: s, segment: c, isHandledByProcessQueue: l } = a, u = this.engineRequest?.segment === c ? this.engineRequest : void 0;
			switch (s) {
				case "loading":
					!e.has(c.runtimeId) && !u && (a.cancel(), this.requests.remove(a));
					break;
				case "succeed":
					if (!o) break;
					o === "http" && this.p2pLoaders.currentLoader.broadcastAnnouncement(), u && (u.resolve(a.data, this.getBandwidth(t)), this.engineRequest = void 0), this.requests.remove(a), this.logger(`succeed: ${J(c)} (byteLength: ${a.data.byteLength})`), this.segmentStorage.storeSegment(n.swarmId, n.streamSwarmId, c.externalId, a.data, c.startTime, c.endTime, c.stream.type, this.streamDetails.isLive);
					break;
				case "failed":
					o === "http" && !l && this.p2pLoaders.currentLoader.broadcastAnnouncement(), !u && !n.segments.has(a.segment.runtimeId) && this.requests.remove(a), a.failedAttempts.httpAttemptsCount >= r && u && (this.engineRequest = void 0, u.reject());
					break;
				case "not-started":
					this.requests.remove(a);
					break;
				case "aborted": this.requests.remove(a);
			}
			a.markHandledByProcessQueue();
			let { lastAttempt: d } = a.failedAttempts;
			d && i - d.error.timestamp > At && a.failedAttempts.clear();
		}
	}
	processQueue() {
		let { queue: e, queueSegmentIds: t, queueDownloadRatio: n } = this.generateQueue();
		this.processRequests(t, n);
		let { simultaneousHttpDownloads: r, simultaneousP2PDownloads: i, httpErrorRetries: a, httpDownloadInitialTimeoutMs: o } = this.config, s = performance.now() - this.createdAt, c = o > 0 && s < o;
		c && (this.initialHttpDelayTimeoutId ??= window.setTimeout(() => {
			this.initialHttpDelayTimeoutId = void 0, this.requestProcessQueueMicrotask();
		}, o - s));
		let { engineRequest: l } = this;
		if (l) {
			let { segment: e } = l, t = this.requests.get(e);
			l.shouldBeStartedImmediately && l.status === "pending" && (!t || t.status === "not-started" || t.status === "failed" || t.status === "aborted") && (!c && (t?.failedAttempts.httpAttemptsCount ?? 0) < a && this.requests.executingHttpCount < r ? this.loadThroughHttp(e) : this.p2pLoaders.currentLoader.isSegmentLoadedBySomeone(e) && this.requests.executingP2PCount < i && this.loadThroughP2P(e));
		}
		for (let t of e) {
			let { statuses: n, segment: o } = t, s = this.requests.get(o);
			if (s?.status !== "succeed") {
				if (n.isHighDemand) {
					let t = !c && (s?.failedAttempts.httpAttemptsCount ?? 0) < a;
					if (s?.status === "loading") {
						t && s.downloadSource === "p2p" && (this.requests.executingHttpCount < r || this.abortLastHttpLoadingInQueueAfterItem(e, o)) && (s.cancel(), this.loadThroughHttp(o));
						continue;
					}
					if (t && (this.requests.executingHttpCount < r || this.abortLastHttpLoadingInQueueAfterItem(e, o))) {
						this.loadThroughHttp(o);
						continue;
					}
					this.p2pLoaders.currentLoader.isSegmentLoadedBySomeone(o) && (this.requests.executingP2PCount < i || this.abortLastP2PLoadingInQueueAfterItem(e, o)) && this.loadThroughP2P(o);
				} else n.isP2PDownloadable && s?.status !== "loading" && this.requests.executingP2PCount < i && this.loadThroughP2P(o);
			}
		}
	}
	abortSegmentRequest(e) {
		this.engineRequest?.segment.runtimeId === e && (this.engineRequest.abort(), this.logger("abort: ", J(this.engineRequest.segment)), this.engineRequest = void 0, this.requestProcessQueueMicrotask());
	}
	loadThroughHttp(e) {
		new ie(this.requests.getOrCreateRequest(e), this.config, this.eventTarget).execute(), this.p2pLoaders.currentLoader.broadcastAnnouncement();
	}
	loadThroughP2P(e) {
		this.p2pLoaders.currentLoader.downloadSegment(e);
	}
	loadRandomThroughHttp() {
		let { httpDownloadInitialTimeoutMs: e } = this.config;
		if (e > 0 && performance.now() - this.createdAt < e) return;
		let t = this.getAvailableStorageCapacityPercent();
		if (t <= 10) return;
		let { simultaneousHttpDownloads: n, httpErrorRetries: r } = this.config, i = this.p2pLoaders.currentLoader;
		if (this.requests.executingHttpCount >= n || !i.connectedPeerCount) return;
		let a = [];
		for (let { segment: e, statuses: n } of kt(this.lastRequestedSegment, this.playback, this.config, this.p2pLoaders.currentLoader, t)) {
			if (!n.isHttpDownloadable || n.isP2PDownloadable || this.segmentStorage.hasSegment(e.stream.swarmId, e.stream.streamSwarmId, e.externalId)) continue;
			let t = this.requests.get(e);
			t && (t.status === "loading" || t.status === "succeed" || t.failedAttempts.httpAttemptsCount >= r) || a.push(e);
		}
		if (!a.length || n - this.requests.executingHttpCount === 0) return;
		let o = i.connectedPeerCount + 1, s = Math.min(a.length, n * o), c = ye(Array.from({ length: s }, (e, t) => t)), l = s / o;
		for (let e of c) {
			if (this.requests.executingHttpCount >= n) break;
			if (l >= 1 || Math.random() <= l) {
				let t = a[e];
				this.loadThroughHttp(t);
			}
			if (l--, l <= 0) break;
		}
	}
	abortLastHttpLoadingInQueueAfterItem(e, t) {
		for (let { segment: n } of he(e)) {
			if (n === t) break;
			let e = this.requests.get(n);
			if (e?.downloadSource === "http" && e.status === "loading") return e.cancel(), !0;
		}
		return !1;
	}
	abortLastP2PLoadingInQueueAfterItem(e, t) {
		for (let { segment: n } of he(e)) {
			if (n === t) break;
			let e = this.requests.get(n);
			if (e?.downloadSource === "p2p" && e.status === "loading") return e.cancel(), !0;
		}
		return !1;
	}
	getAvailableStorageCapacityPercent() {
		let { totalCapacity: e, usedCapacity: t } = this.segmentStorage.getUsage();
		return 100 - t / e * 100;
	}
	generateQueue() {
		let e = [], t = /* @__PURE__ */ new Set(), n = 0, r = 0, i = this.getAvailableStorageCapacityPercent();
		for (let a of kt(this.lastRequestedSegment, this.playback, this.config, this.p2pLoaders.currentLoader, i)) {
			n++;
			let { segment: i } = a;
			if (this.segmentStorage.hasSegment(i.stream.swarmId, i.stream.streamSwarmId, i.externalId) || this.requests.get(i)?.status === "succeed") {
				r++;
				continue;
			}
			e.push(a), t.add(i.runtimeId);
		}
		return {
			queue: e,
			queueSegmentIds: t,
			maxPossibleLength: n,
			alreadyLoadedCount: r,
			queueDownloadRatio: n === 0 ? 0 : r / n
		};
	}
	getBandwidth(e) {
		let { http: t, all: n } = this.bandwidthCalculators, { activeLevelBitrate: r } = this.streamDetails;
		if (r === 0) return n.getBandwidthLoadingOnly(3);
		let i = Math.max(n.getBandwidth(30, this.levelChangedTimestamp), n.getBandwidth(60, this.levelChangedTimestamp), n.getBandwidth(90, this.levelChangedTimestamp));
		if (e >= .8 || i >= r * .9) return Math.max(n.getBandwidthLoadingOnly(1), n.getBandwidthLoadingOnly(3), n.getBandwidthLoadingOnly(5));
		let a = Math.max(t.getBandwidthLoadingOnly(1), t.getBandwidthLoadingOnly(3), t.getBandwidthLoadingOnly(5));
		return Math.max(i, a);
	}
	notifyLevelChanged() {
		this.levelChangedTimestamp = performance.now();
	}
	sendBroadcastAnnouncement(e = !1) {
		this.p2pLoaders.currentLoader.broadcastAnnouncement(e);
	}
	updatePlayback(e, t) {
		let n = this.playback.rate !== t, r = this.playback.position !== e;
		if (!n && !r) return;
		let i = Math.abs(e - this.playback.position) / this.segmentAvgDuration > .5;
		r && (this.playback.position = e), n && t !== 0 && (this.playback.rate = t), i && (this.logger("position significantly changed"), this.engineRequest?.markAsShouldBeStartedImmediately()), this.segmentStorage.onPlaybackUpdated(e, t), this.requestProcessQueueMicrotask(i);
	}
	updateStream(e) {
		e === this.lastRequestedSegment.stream && (this.logger(`update stream: ${q(e)}`), this.requestProcessQueueMicrotask());
	}
	destroy() {
		clearTimeout(this.randomHttpDownloadTimeout), clearTimeout(this.initialHttpDelayTimeoutId), this.engineRequest?.abort(), this.requests.destroy(), this.p2pLoaders.destroy();
	}
};
//#endregion
//#region src/utils/hash.ts
function Nt(e) {
	let t = me(e), n = [], r = t.length * 8;
	for (let e = 0; e < t.length; e++) n[e >> 2] |= (t[e] & 255) << 24 - e % 4 * 8;
	n[r >> 5] |= 128 << 24 - r % 32, n[(r + 64 >> 9 << 4) + 15] = r;
	let i = 1732584193, a = 4023233417, o = 2562383102, s = 271733878, c = 3285377520, l = [];
	for (let e = 0; e < n.length; e += 16) {
		let t = i, r = a, u = o, d = s, f = c;
		for (let t = 0; t < 80; t++) {
			if (t < 16) l[t] = n[e + t] | 0;
			else {
				let e = l[t - 3] ^ l[t - 8] ^ l[t - 14] ^ l[t - 16];
				l[t] = e << 1 | e >>> 31;
			}
			let r;
			r = t < 20 ? (a & o | ~a & s) + 1518500249 : t < 40 ? (a ^ o ^ s) + 1859775393 : t < 60 ? (a & o | a & s | o & s) - 1894007588 : (a ^ o ^ s) - 899497514;
			let u = (i << 5 | i >>> 27) + c + (l[t] >>> 0) + r | 0;
			c = s, s = o, o = a << 30 | a >>> 2, a = i, i = u;
		}
		i = i + t | 0, a = a + r | 0, o = o + u | 0, s = s + d | 0, c = c + f | 0;
	}
	let u = "", d = [
		i,
		a,
		o,
		s,
		c
	];
	for (let e = 0; e < 20; e++) {
		let t = 24 - e % 4 * 8, n = d[e >> 2];
		u += String.fromCharCode(n >>> t & 255);
	}
	return u;
}
//#endregion
//#region src/stream-identity.ts
var Pt = "v2";
function Z({ bitrate: e, codecs: t, width: n, height: r, language: i, channels: a, name: o, frameRate: s, videoRange: c }) {
	let l = t ? t.split(",").map((e) => {
		e = e.trim().toLowerCase();
		let t = e.split(".");
		if (t.length === 3 && (t[0] === "avc1" || t[0] === "avc")) {
			let n = parseInt(t[1], 10), r = parseInt(t[2], 10);
			if (!isNaN(n) && !isNaN(r) && t[1] === n.toString() && t[2] === r.toString()) {
				let i = `00${n.toString(16)}`.slice(-2), a = `00${r.toString(16)}`.slice(-2);
				e = `${t[0]}.${i}00${a}`;
			}
		}
		return e;
	}).sort().join(",") : "", u = i && i !== "und" ? i.slice(0, 2).toLowerCase() : "", d = a ? a.toString().split("/")[0] : "", f = o ? o.toLowerCase().trim() : "", p = s && !isNaN(Number(s)) ? Number(s).toString() : "", m = c ? c.toUpperCase().trim() : "", h = `${e ?? 0}-${l}-${n ?? ""}-${r ?? ""}-${u}-${d}-${f}-${p}-${m}`;
	return btoa(Nt(h));
}
function Ft(e, t, n) {
	return `v2-${e}-${t}-${n}`;
}
function It(e) {
	return Ft(e.swarmId, e.streamType, Z(e.properties));
}
function Lt(e) {
	return btoa(Nt(e).slice(0, 15));
}
//#endregion
//#region src/segment-storage/utils.ts
var Q = (e, t) => `${e}|${t}`, Rt = (e) => /Android/i.test(e), zt = (e) => /iPad|iPhone/i.test(e), Bt = (e) => /Android/i.test(e) && (/; wv\)/i.test(e) || !/Chrome|Firefox/i.test(e)), $ = 1048576, Vt = class {
	userAgent = navigator.userAgent;
	segmentMemoryStorageLimit = 4096;
	currentStorageUsage = 0;
	cache = /* @__PURE__ */ new Map();
	logger;
	coreConfig;
	mainStreamConfig;
	secondaryStreamConfig;
	currentPlayback;
	lastRequestedSegment;
	segmentChangeCallback;
	constructor() {
		this.logger = (0, v.default)("p2pml-core:segment-memory-storage"), this.logger.color = "RebeccaPurple";
	}
	async initialize(e, t, n) {
		this.coreConfig = e, this.mainStreamConfig = t, this.secondaryStreamConfig = n, this.setMemoryStorageLimit(), this.logger("initialized");
	}
	onPlaybackUpdated(e, t) {
		this.currentPlayback = {
			position: e,
			rate: t
		};
	}
	onSegmentRequested(e, t, n, r, i, a, o) {
		this.lastRequestedSegment = {
			streamSwarmId: t,
			segmentId: n,
			startTime: r,
			endTime: i,
			swarmId: e,
			streamType: a,
			isLiveStream: o
		};
	}
	async storeSegment(e, t, n, r, i, a, o, s) {
		this.clear(s, r.byteLength);
		let c = Q(t, n);
		if (this.cache.set(c, {
			data: r,
			segmentId: n,
			streamSwarmId: t,
			startTime: i,
			endTime: a,
			streamType: o
		}), this.increaseStorageUsage(r.byteLength), this.logger(`add segment: ${n} to ${t}`), !this.segmentChangeCallback) throw Error("dispatchStorageUpdatedEvent is not set");
		this.segmentChangeCallback(t);
	}
	async getSegmentData(e, t, n) {
		let r = Q(t, n), i = this.cache.get(r);
		if (i !== void 0) return i.data;
	}
	getUsage() {
		if (!this.lastRequestedSegment || !this.currentPlayback) return {
			totalCapacity: this.segmentMemoryStorageLimit,
			usedCapacity: this.currentStorageUsage
		};
		let e = this.currentPlayback.position, t = 0;
		for (let { endTime: n, data: r } of this.cache.values()) e > n || (t += r.byteLength);
		return {
			totalCapacity: this.segmentMemoryStorageLimit,
			usedCapacity: t / $
		};
	}
	hasSegment(e, t, n) {
		let r = Q(t, n);
		return this.cache.get(r) !== void 0;
	}
	getStoredSegmentIds(e, t) {
		let n = [];
		for (let { segmentId: e, streamSwarmId: r } of this.cache.values()) r === t && n.push(e);
		return n;
	}
	clear(e, t) {
		if (!this.currentPlayback || !this.mainStreamConfig || !this.secondaryStreamConfig || !this.coreConfig || !this.isMemoryLimitReached(t) && !e) return;
		let n = /* @__PURE__ */ new Set(), r = Array.from(this.cache.values()).sort((e, t) => e.startTime - t.startTime);
		for (let i of r) {
			let { streamSwarmId: r, segmentId: a, data: o } = i, s = Q(r, a);
			if (this.shouldRemoveSegment(i, e, this.currentPlayback.position) && (this.cache.delete(s), n.add(r), this.decreaseStorageUsage(o.byteLength), this.logger(`Removed segment ${a} from stream ${r}`), !this.isMemoryLimitReached(t) && !e)) break;
		}
		this.sendUpdatesToAffectedStreams(n);
	}
	isMemoryLimitReached(e) {
		return this.currentStorageUsage + e / $ > this.segmentMemoryStorageLimit;
	}
	setSegmentChangeCallback(e) {
		this.segmentChangeCallback = e;
	}
	sendUpdatesToAffectedStreams(e) {
		e.size !== 0 && e.forEach((e) => {
			if (!this.segmentChangeCallback) throw Error("dispatchStorageUpdatedEvent is not set");
			this.segmentChangeCallback(e);
		});
	}
	shouldRemoveSegment(e, t, n) {
		let { endTime: r, streamType: i } = e, a = this.getStreamTimeWindow(i, "highDemandTimeWindow");
		return n <= r ? !1 : !t || n > a + r;
	}
	increaseStorageUsage(e) {
		this.currentStorageUsage += e / $;
	}
	decreaseStorageUsage(e) {
		this.currentStorageUsage -= e / $;
	}
	setMemoryStorageLimit() {
		if (this.coreConfig?.segmentMemoryStorageLimit) {
			this.segmentMemoryStorageLimit = this.coreConfig.segmentMemoryStorageLimit;
			return;
		}
		Bt(this.userAgent) || zt(this.userAgent) ? this.segmentMemoryStorageLimit = 1024 : Rt(this.userAgent) && (this.segmentMemoryStorageLimit = 2048);
	}
	getStreamTimeWindow(e, t) {
		return (e === "main" ? this.mainStreamConfig : this.secondaryStreamConfig)?.[t] ?? 0;
	}
	destroy() {
		this.cache.clear(), this.segmentChangeCallback = void 0;
	}
}, Ht = `-PM${Kt("4.0.0")}-`, Ut = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789", Wt = 20;
function Gt(e) {
	let t = [e], n = Wt - e.length;
	for (let e = 0; e < n; e++) t.push(Ut[Math.floor(Math.random() * 62)]);
	return t.join("");
}
function Kt(e) {
	let t = e.split(".");
	return `${`00${t[0]}`.slice(-2)}${`00${t[1]}`.slice(-2)}`;
}
//#endregion
//#region src/webtorrent/websocket-client/index.ts
var qt = class {
	#e;
	#t = "disconnected";
	#n = null;
	#r = 0;
	#i = null;
	#a = new V();
	constructor(e) {
		let t = Math.max(100, e.initialDelay ?? 1e3);
		this.#e = {
			url: e.url,
			initialDelay: t,
			maxDelay: Math.max(t, e.maxDelay ?? 3e4),
			jitterMultiplier: Math.max(0, e.jitterMultiplier ?? .2)
		};
	}
	get state() {
		return this.#t;
	}
	addEventListener(e, t) {
		this.#a.addEventListener(e, t);
	}
	removeEventListener(e, t) {
		this.#a.removeEventListener(e, t);
	}
	connect() {
		if (this.#t !== "connected" && this.#t !== "connecting" && this.#t !== "disposed") {
			this.#t = "connecting", this.#d();
			try {
				this.#n = new WebSocket(this.#e.url), this.#n.binaryType = "arraybuffer", this.#n.onopen = this.#o, this.#n.onclose = this.#s, this.#n.onerror = this.#c, this.#n.onmessage = this.#l;
			} catch (e) {
				this.#t = "disconnected";
				let t = new ErrorEvent("error", {
					message: e instanceof Error ? e.message : "Unknown WebSocket creation error",
					error: e
				});
				this.#a.dispatchEvent("error", t);
			}
		}
	}
	send(e) {
		if (this.#t !== "connected" || !this.#n) throw Error("WebSocketClient: Cannot send data when not connected");
		this.#n.send(e);
	}
	dispose() {
		this.#t = "disposed", this.#d(), this.#n &&= (this.#n.onopen = null, this.#n.onclose = null, this.#n.onerror = null, this.#n.onmessage = null, this.#n.close(), null), this.#a.clear();
	}
	#o = () => {
		this.#t !== "disposed" && (this.#t = "connected", this.#r = 0, this.#a.dispatchEvent("connected"));
	};
	#s = () => {
		this.#t !== "disposed" && (this.#n &&= (this.#n.onopen = null, this.#n.onclose = null, this.#n.onerror = null, this.#n.onmessage = null, null), this.#u(), this.#a.dispatchEvent("disconnected"));
	};
	#c = (e) => {
		this.#t !== "disposed" && this.#a.dispatchEvent("error", e);
	};
	#l = (e) => {
		this.#t !== "disposed" && this.#a.dispatchEvent("message", e.data);
	};
	#u() {
		if (this.#t === "disposed") return;
		this.#t = "reconnecting";
		let e = Math.min(this.#e.initialDelay * 2 ** this.#r, this.#e.maxDelay), t = e * this.#e.jitterMultiplier, n = Math.random() * 2 * t - t, r = Math.max(0, e + n);
		e < this.#e.maxDelay && this.#r++, this.#i = setTimeout(() => {
			this.#t !== "disposed" && this.connect();
		}, r), this.#a.dispatchEvent("reconnecting");
	}
	#d() {
		this.#i !== null && (clearTimeout(this.#i), this.#i = null);
	}
}, Jt = class {
	#e = /* @__PURE__ */ new Map();
	#t = new V();
	addEventListener(e, t) {
		this.#t.addEventListener(e, t);
	}
	removeEventListener(e, t) {
		this.#t.removeEventListener(e, t);
	}
	acquire(e) {
		let t = this.#e.get(e);
		if (!t) {
			let n = new qt({ url: e });
			n.addEventListener("error", (t) => {
				this.#t.dispatchEvent("error", t, e);
			}), n.connect(), t = {
				client: n,
				refCount: 0
			}, this.#e.set(e, t);
		}
		t.refCount++;
		let n = !1;
		return {
			client: t.client,
			release: () => {
				n || (n = !0, t.refCount--, t.refCount <= 0 && (t.refCount < 0 && console.error(`[WebTorrentSocketPool] Negative refCount detected for ${e}`), this.#e.get(e) === t && this.#e.delete(e), t.client.dispose()));
			}
		};
	}
	destroy() {
		this.#t.clear();
		let e = Array.from(this.#e.values());
		this.#e.clear();
		for (let t of e) try {
			t.client.dispose();
		} catch (e) {
			console.error("[WebTorrentSocketPool] Failed to dispose WebSocketClient:", e);
		}
	}
}, Yt = class e {
	static DEFAULT_COMMON_CORE_CONFIG = {
		segmentMemoryStorageLimit: void 0,
		customSegmentStorageFactory: void 0,
		trackerClientVersionPrefix: Ht
	};
	static DEFAULT_STREAM_CONFIG = {
		isP2PUploadDisabled: !1,
		isP2PDisabled: !1,
		simultaneousHttpDownloads: 2,
		simultaneousP2PDownloads: 3,
		highDemandTimeWindow: 15,
		httpDownloadInitialTimeoutMs: 0,
		httpDownloadTimeWindow: 3e3,
		p2pDownloadTimeWindow: 6e3,
		webRtcMaxMessageSize: 65535,
		p2pNotReceivingBytesTimeoutMs: 2e3,
		p2pInactiveLoaderDestroyTimeoutMs: 3e4,
		httpNotReceivingBytesTimeoutMs: 3e3,
		httpErrorRetries: 3,
		p2pErrorRetries: 3,
		announceTrackers: [
			"wss://tracker.novage.com.ua",
			"wss://tracker.webtorrent.dev",
			"wss://tracker.openwebtorrent.com"
		],
		rtcConfig: { iceServers: [{ urls: "stun:stun.l.google.com:19302" }, { urls: "stun:global.stun.twilio.com:3478" }] },
		validateP2PSegment: void 0,
		validateHTTPSegment: void 0,
		httpRequestSetup: void 0,
		swarmId: void 0,
		streamSwarmIdBuilder: void 0,
		p2pMaxPeers: 50,
		p2pChurnMaxPeersMultiplier: 1.5,
		p2pChurnCleanupIntervalMs: 3e4,
		p2pChurnGracePeriodMs: 15e3,
		webRtcOffersCount: 5,
		webRtcOfferTimeoutMs: 5e4,
		webRtcIceGatheringTimeoutMs: 5e3,
		webRtcConnectionTimeoutMs: 15e3
	};
	eventTarget = new V();
	manifestResponseUrl;
	streams = /* @__PURE__ */ new Map();
	mainStreamConfig;
	secondaryStreamConfig;
	commonCoreConfig;
	bandwidthCalculators = {
		all: new z(),
		http: new z()
	};
	segmentStorage;
	webTorrentSocketPool = new Jt();
	logger = (0, v.default)("p2pml-core:core");
	socketPoolLogger = (0, v.default)("p2pml-core:webtorrent-socket-pool");
	peerId;
	mainStreamLoader;
	secondaryStreamLoader;
	streamDetails = {
		isLive: !1,
		activeLevelBitrate: 0
	};
	storageInitPromise;
	constructor(t) {
		let n = ve(t ?? {});
		this.commonCoreConfig = S({
			defaultConfig: e.DEFAULT_COMMON_CORE_CONFIG,
			baseConfig: n
		}), this.mainStreamConfig = S({
			defaultConfig: e.DEFAULT_STREAM_CONFIG,
			baseConfig: n,
			specificStreamConfig: n.mainStream
		}), this.secondaryStreamConfig = S({
			defaultConfig: e.DEFAULT_STREAM_CONFIG,
			baseConfig: n,
			specificStreamConfig: n.secondaryStream
		}), this.peerId = Gt(this.commonCoreConfig.trackerClientVersionPrefix), this.webTorrentSocketPool.addEventListener("error", (e, t) => {
			this.socketPoolLogger(`WebSocket error for tracker url ${t}:`, e);
		});
	}
	getConfig() {
		return {
			...b(this.commonCoreConfig),
			mainStream: b(this.mainStreamConfig),
			secondaryStream: b(this.secondaryStreamConfig)
		};
	}
	applyDynamicConfig(e) {
		let { mainStream: t, secondaryStream: n } = e, r = b(this.mainStreamConfig), i = b(this.secondaryStreamConfig);
		this.overrideAllConfigs(e, t, n), this.processSpecificDynamicConfigParams(r, e, "main"), this.processSpecificDynamicConfigParams(i, e, "secondary");
	}
	processSpecificDynamicConfigParams(e, t, n) {
		let r = this.getUpdatedStreamProperty("isP2PDisabled", t, n);
		r && e.isP2PDisabled !== r && this.destroyStreamLoader(n);
		let i = this.getUpdatedStreamProperty("isP2PUploadDisabled", t, n);
		i !== void 0 && e.isP2PUploadDisabled !== i && (n === "main" ? this.mainStreamLoader : this.secondaryStreamLoader)?.sendBroadcastAnnouncement(i);
	}
	getUpdatedStreamProperty(e, t, n) {
		return (n === "main" ? t.mainStream : t.secondaryStream)?.[e] ?? t[e];
	}
	addEventListener(e, t) {
		this.eventTarget.addEventListener(e, t);
	}
	removeEventListener(e, t) {
		this.eventTarget.removeEventListener(e, t);
	}
	setManifestResponseUrl(e) {
		this.manifestResponseUrl = e.split("?")[0];
	}
	hasSegment(e) {
		return !!gt(this.streams, e);
	}
	getStream(e) {
		let t = this.streams.get(e);
		return t && this.toStreamSnapshot(t);
	}
	getStreamSegmentRuntimeIds(e) {
		let t = this.streams.get(e);
		if (t) return new Set(t.segments.keys());
	}
	getStreams() {
		return Array.from(this.streams.values(), (e) => this.toStreamSnapshot(e));
	}
	toStreamSnapshot(e) {
		let { segments: t, ...n } = e;
		return n;
	}
	addStreamIfNoneExists(e) {
		if (this.streams.has(e.runtimeId)) return;
		let t = Object.freeze({ ...e.properties });
		try {
			this.registerStream(e, t);
		} catch (n) {
			this.logger("failed to register stream %s: %O", e.runtimeId, n), this.eventTarget.dispatchEvent("onStreamRegistrationError", {
				runtimeId: e.runtimeId,
				streamType: e.type,
				properties: t,
				error: n instanceof Error ? n : Error(String(n))
			});
		}
	}
	registerStream(e, t) {
		let n = e.type === "main" ? this.mainStreamConfig : this.secondaryStreamConfig, r = n.swarmId ?? this.manifestResponseUrl;
		if (r === void 0) throw Error("Failed to register stream: no swarmId is configured and the manifest response URL is not set. Call setManifestResponseUrl() before adding streams.");
		let i = Z(t), a = Ft(r, e.type, i);
		if (n.streamSwarmIdBuilder) {
			let o = n.streamSwarmIdBuilder({
				swarmId: r,
				runtimeId: e.runtimeId,
				streamType: e.type,
				properties: t,
				identityHash: i,
				defaultStreamSwarmId: a,
				peerProtocolVersion: "v2"
			});
			if (o !== void 0) {
				if (typeof o != "string" || o === "") throw Error("streamSwarmIdBuilder must return a non-empty string or undefined");
				a = o;
			}
		}
		for (let t of this.streams.values()) if (t.streamSwarmId === a && (t.swarmId !== r || t.type !== e.type || t.identityHash !== i)) throw Error(`streamSwarmIdBuilder produced the same stream swarm ID ("${a}") for streams with different identities. Peers of these streams would exchange segments of the wrong stream.`);
		let o = {
			...e,
			properties: t,
			swarmId: r,
			identityHash: i,
			streamSwarmId: a,
			infoHash: Lt(a),
			segments: /* @__PURE__ */ new Map()
		};
		this.streams.set(e.runtimeId, o), this.eventTarget.dispatchEvent("onStreamAdded", { stream: this.toStreamSnapshot(o) });
	}
	updateStream(e, t, n) {
		let r = this.streams.get(e);
		if (r) {
			if (t) for (let e of t) r.segments.has(e.runtimeId) || r.segments.set(e.runtimeId, {
				...e,
				stream: r
			});
			if (n) for (let e of n) r.segments.delete(e);
			this.mainStreamLoader?.updateStream(r), this.secondaryStreamLoader?.updateStream(r);
		}
	}
	async loadSegment(e, t) {
		await this.initializeSegmentStorage();
		let n = this.identifySegment(e);
		this.getStreamHybridLoader(n).loadSegment(n, t);
	}
	abortSegmentLoading(e) {
		this.mainStreamLoader?.abortSegmentRequest(e), this.secondaryStreamLoader?.abortSegmentRequest(e);
	}
	updatePlayback(e, t) {
		this.mainStreamLoader?.updatePlayback(e, t), this.secondaryStreamLoader?.updatePlayback(e, t);
	}
	setActiveLevelBitrate(e) {
		e !== this.streamDetails.activeLevelBitrate && (this.streamDetails.activeLevelBitrate = e, this.mainStreamLoader?.notifyLevelChanged(), this.secondaryStreamLoader?.notifyLevelChanged());
	}
	setIsLive(e) {
		this.streamDetails.isLive = e;
	}
	isSegmentLoadable(e) {
		try {
			let t = this.identifySegment(e);
			return !(t.stream.type === "main" && this.mainStreamConfig.isP2PDisabled || t.stream.type === "secondary" && this.secondaryStreamConfig.isP2PDisabled);
		} catch {
			return !1;
		}
	}
	destroy() {
		this.streams.clear(), this.mainStreamLoader?.destroy(), this.secondaryStreamLoader?.destroy(), this.segmentStorage?.setSegmentChangeCallback(void 0), this.segmentStorage?.destroy(), this.mainStreamLoader = void 0, this.secondaryStreamLoader = void 0, this.segmentStorage = void 0, this.manifestResponseUrl = void 0, this.streamDetails = {
			isLive: !1,
			activeLevelBitrate: 0
		}, this.storageInitPromise = void 0, this.webTorrentSocketPool.destroy();
	}
	async initializeSegmentStorage() {
		if (!this.segmentStorage) {
			if (this.storageInitPromise) return this.storageInitPromise;
			this.storageInitPromise = (async () => {
				let { isLive: e } = this.streamDetails, t = this.commonCoreConfig.customSegmentStorageFactory;
				if (t && typeof t != "function") throw Error("Storage configuration is invalid");
				let n = t ? t(e) : new Vt();
				try {
					await n.initialize(this.commonCoreConfig, this.mainStreamConfig, this.secondaryStreamConfig);
				} catch (e) {
					throw n.destroy(), e;
				}
				if (!this.storageInitPromise) {
					n.setSegmentChangeCallback(void 0), n.destroy();
					return;
				}
				n.setSegmentChangeCallback((e) => {
					this.eventTarget.dispatchEvent(`onStorageUpdated-${e}`);
				}), this.segmentStorage = n;
			})();
			try {
				await this.storageInitPromise;
			} finally {
				this.storageInitPromise = void 0;
			}
		}
	}
	identifySegment(e) {
		let t = gt(this.streams, e);
		if (!t) throw Error(`Not found segment with id: ${e}`);
		return t;
	}
	overrideAllConfigs(e, t, n) {
		let r = this.stripStaticOnlyProps(e), i = t && this.stripStaticOnlyProps(t), a = n && this.stripStaticOnlyProps(n);
		x(this.commonCoreConfig, r), x(this.mainStreamConfig, r), x(this.secondaryStreamConfig, r), i && x(this.mainStreamConfig, i), a && x(this.secondaryStreamConfig, a);
	}
	stripStaticOnlyProps(e) {
		if (!("swarmId" in e) && !("streamSwarmIdBuilder" in e)) return e;
		this.logger("swarmId and streamSwarmIdBuilder cannot be changed at runtime; ignoring them in the dynamic configuration update");
		let t = { ...e };
		return delete t.swarmId, delete t.streamSwarmIdBuilder, t;
	}
	destroyStreamLoader(e) {
		e === "main" ? (this.mainStreamLoader?.destroy(), this.mainStreamLoader = void 0) : (this.secondaryStreamLoader?.destroy(), this.secondaryStreamLoader = void 0);
	}
	getStreamHybridLoader(e) {
		return e.stream.type === "main" ? (this.mainStreamLoader ??= this.createNewHybridLoader(e), this.mainStreamLoader) : (this.secondaryStreamLoader ??= this.createNewHybridLoader(e), this.secondaryStreamLoader);
	}
	createNewHybridLoader(e) {
		if (!this.segmentStorage) throw Error("Segment storage is not initialized");
		let t = e.stream.type === "main" ? this.mainStreamConfig : this.secondaryStreamConfig;
		return new Mt(e, this.streamDetails, t, this.bandwidthCalculators, this.segmentStorage, this.webTorrentSocketPool, this.eventTarget, this.peerId);
	}
}, Xt = v.debug;
export { Yt as Core, _ as CoreRequestError, Pt as PEER_PROTOCOL_VERSION, h as PeerConnectError, d as PeerError, f as PeerWarning, g as RequestError, p as TrackerError, m as TrackerWarning, u as TypedError, Ft as buildStreamSwarmId, Lt as computeInfoHash, Z as computeStreamIdentityHash, It as computeStreamSwarmId, Xt as debug };

//# sourceMappingURL=p2p-media-loader-core.es.min.js.map